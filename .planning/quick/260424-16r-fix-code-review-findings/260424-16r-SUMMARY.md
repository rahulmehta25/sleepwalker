# Quick Task 260424-16r: Fix Code Review Findings Summary

**One-liner:** Five code-review fixes — queue.ts flock lock, listBundles runtime filter, Zod settings validation, frozen-surface compat gate for audit.ts, and ESLint 9 flat config.

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| 1 | queue.ts race condition — `updateLocalStatus` now acquires `proper-lockfile` lockSync before read-modify-write | e7f6771 |
| 2a | listBundles optional `runtime?` parameter added | eccaae1 |
| 2b | Zod `SettingsSchema` added to settings.ts; POST /api/settings validates before write | a8d237d |
| 3a | frozen-surface.sh: audit.ts moved from Group A to Group B with v0.2 predicate | cee4c35 |
| 3a-fix | audit.ts: added `RunAuditEntry` interface and `readRunsByFleet` function (required by compat gate) | eb00980 |
| 3b | ESLint 9 flat config (`eslint.config.mjs`) + devDependencies for eslint/eslint-config-next/@eslint/eslintrc | f351eec |
| 3b-lock | pnpm-lock.yaml updated for new ESLint deps | 244450e |

## Verification Results

- `pnpm --dir dashboard typecheck` — PASS (no TS errors)
- `pnpm --dir dashboard test` — PASS (413 tests, 48 test files)
- `bash tests/compat/frozen-surface.sh` — PASS (COMP-02 PASS: frozen surface intact vs 998455b)
- `pnpm lint` (from worktree dashboard) — EXIT 0 (one react-hooks/exhaustive-deps warning in pre-existing cloud-client.tsx, not an error)

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical functionality] audit.ts missing v0.2 RunAuditEntry + readRunsByFleet**
- **Found during:** Task 3a — after writing the compat gate predicate that checks for these symbols, the worktree's audit.ts was missing them (the worktree branched before those additions landed in main)
- **Fix:** Brought the v0.2 additive extensions from the main branch into the worktree's audit.ts — identical to what the main project already had
- **Files modified:** `dashboard/lib/audit.ts`
- **Commit:** eb00980

**2. [Rule 3 - Blocking issue] pnpm-lock.yaml out of sync after ESLint dep addition**
- **Found during:** Task 3b — running lint from the worktree required installing deps locally since the worktree has no node_modules symlink to the main project
- **Fix:** Ran `pnpm install` in worktree dashboard to resolve and commit updated lockfile
- **Files modified:** `dashboard/pnpm-lock.yaml`
- **Commit:** 244450e

## Self-Check

- [x] `dashboard/lib/queue.ts` — exists, lockSync import present
- [x] `dashboard/lib/bundles.ts` — exists, `runtime?: Runtime` parameter present
- [x] `dashboard/lib/settings.ts` — exists, `SettingsSchema` export present
- [x] `dashboard/app/api/settings/route.ts` — exists, `safeParse` validation present
- [x] `tests/compat/frozen-surface.sh` — audit.ts removed from GROUP_A, assert_exception_audit_ts added
- [x] `dashboard/lib/audit.ts` — RunAuditEntry + readRunsByFleet present
- [x] `dashboard/eslint.config.mjs` — created
- [x] `dashboard/package.json` — eslint devDeps present

## Self-Check: PASSED
