# Roadmap: Sleepwalker v0.2 — Multi-Runtime Agent Deployment

**Created:** 2026-04-18
**Granularity:** standard (5-8 phases, 3-5 plans each)
**Mode:** yolo
**Core Value:** Write a prompt + pick a schedule + pick a runtime -> click once -> a live agent exists on that runtime, scheduled, audited, and reviewed from one place.

## Phases

- [x] **Phase 1: Foundation** - Freeze the adapter interface and slug-namespacing conventions that unblock all parallel work
- [x] **Phase 2: Adapters** - Ship four runtime adapters + supervisor + launchd writer so every target runtime can be deployed and probed — **code complete 2026-04-19** (2 manual smokes pending user execution; contracts at `test/manual/*-smoke.md`)
- [x] **Phase 3: Editor** - `/editor` form writes validated, secret-scanned routine bundles to disk with autosave and collision protection — **completed 2026-04-19** (9/9 plans, dashboard suite 250/250 green, frozen-surface diff 0 lines vs PHASE3_BASE `104547f`)
- [x] **Phase 4: Deploy** - One-click deploy state machine + Run-now + Save-to-repo + health badges wire adapters through the dashboard UI — **completed 2026-04-20** (9/9 plans, dashboard suite 336/336 green, supervisor harness 28/0, frozen-surface diff 0 lines vs PHASE4_BASE `8707433^`, all 7 requirements DEPL-01..05 + REPO-01 + HLTH-01 Complete)
- [x] **Phase 5: Queue** - Codex + Gemini runs flow into the Morning Queue with ANSI-stripped, race-free, flock-protected audit — **completed 2026-04-21** (8/8 plans; dashboard suite 358/358 across 40 files; supervisor-tests 36/36 across 30 scenarios; run-tests 29/29; frozen-surface diff 0 lines vs PHASE5_BASE `3c81b4f^` = `37ab5d9`; 3 documented QUEU-04 exceptions on v0.1 surface audited additive-only; QUEU-01 + QUEU-02 + QUEU-03 + QUEU-04 + SAFE-01 all Complete; v0.1 CONCERNS.md §concurrent-JSONL race CLOSED)
- [ ] **Phase 6: Polish** - OSS docs, per-runtime templates, diagnostics page, and backward-compat integration gate

## Phase Details

### Phase 1: Foundation
**Goal**: Freeze the `RuntimeAdapter` TypeScript interface and cross-runtime naming conventions so all adapter, editor, and deploy work can proceed in parallel without interface churn.
**Depends on**: Nothing (first phase)
**Requirements**: ADPT-01, ADPT-02
**Success Criteria** (what must be TRUE):
  1. A TypeScript consumer can `import { RuntimeAdapter, RoutineBundle, DeployResult, HealthStatus } from "dashboard/lib/runtime-adapters/types"` and the interface contract compiles without modification through the rest of v0.2.
  2. Every new identifier in the system (launchd label, marker tag, branch name, internal key) resolves to `<runtime>/<slug>` form; a Codex `daily-brief` and Gemini `daily-brief` never collide anywhere in state, logs, or git.
  3. `routines-codex/` and `routines-gemini/` directories exist as additive siblings; all four v0.1 routine paths (`routines-local/`, `routines-cloud/`, hooks, install.sh) remain byte-identical.
**Plans**: 4 plans

Plans:
- [x] 01-01-PLAN.md -- types.ts interface freeze + index.ts registry skeleton (ADPT-01) — **completed 2026-04-18** (commit c146acf)
- [x] 01-02-PLAN.md -- routines-codex/, routines-gemini/, templates/ .gitkeep scaffolding (ADPT-02) — **completed 2026-04-18** (commit b38416c)
- [x] 01-03-PLAN.md -- slug.ts validator + 7 builders + slug.test.ts 28 assertions (ADPT-02) — **completed 2026-04-18** (commits 313bf62 / fbe8adc / 8b73e0f)
- [x] 01-04-PLAN.md -- frozen-surface gate + full-suite verification (ADPT-01 + ADPT-02) — **completed 2026-04-18** (commit b924c9a)

### Phase 2: Adapters
**Goal**: Ship a working, health-checkable, safely-invoked implementation of `RuntimeAdapter` for all four runtimes, plus the shared launchd writer and `bin/sleepwalker-run-cli` supervisor that the Codex and Gemini adapters depend on.
**Depends on**: Phase 1
**Requirements**: ADPT-03, ADPT-04, ADPT-05, ADPT-06, ADPT-07, ADPT-08, ADPT-09, SAFE-02
**Success Criteria** (what must be TRUE):
  1. Calling `getAdapter("codex").deploy(bundle)` writes a `plutil -lint`-valid plist with an absolute `codex` binary path (resolved via login shell) and an explicit `PATH` env; `launchctl bootstrap gui/$UID` loads it; on a fresh boot 8 hours later, launchd fires the job and finds the binary.
  2. Calling `getAdapter("gemini").deploy(bundle)` produces a plist with an explicit `GOOGLE_CLOUD_PROJECT` env var; `healthCheck()` reports the active auth mode and quota project so users see auth collisions before they burn quota.
  3. The supervisor script, when invoked by launchd, strips ANSI, routes the prompt via file (never argv), enforces the character budget with SIGTERM on exceed, and emits audit entries regardless of whether the wrapped CLI exits 0.
  4. `healthCheckAll()` returns four `HealthStatus` objects where unavailable runtimes report `{available: false, reason: "..."}` and never throw; a fresh Mac missing `codex` still loads the dashboard.
  5. Every consumer in v0.2 reaches runtimes through `getAdapter(runtime)`; no file in `dashboard/` directly imports a specific adapter module.
**Plans**: 10 plans

Plans:
- [x] 02-01-PLAN.md -- slug.ts assertValidSlug guard + slug.test.ts throw coverage (ADPT-02 amend) — **completed 2026-04-19** (commit c5922de)
- [x] 02-02-PLAN.md -- launchd-writer.ts plist generator + install/uninstall + 9 Vitest blocks (ADPT-03) — **completed 2026-04-19** (commit e14bbe6)
- [x] 02-03-PLAN.md -- bin/sleepwalker-run-cli supervisor (183 bash) (ADPT-04 + SAFE-02) — **completed 2026-04-19** (commit 39f7eb3)
- [x] 02-04-PLAN.md -- supervisor-tests.sh harness 6 scenarios (ADPT-04 verification) — **completed 2026-04-19** (commit 5bdb19c)
- [x] 02-05-PLAN.md -- claude-routines.ts adapter + 7 tests (ADPT-05) — **completed 2026-04-19** (commits 62bdaa7 + d7223a8)
- [x] 02-06-PLAN.md -- claude-desktop.ts adapter + 6 tests (ADPT-06) — **completed 2026-04-19** (commit 81f68ca)
- [x] 02-07-PLAN.md -- codex.ts adapter + 6 tests (ADPT-07) — **completed 2026-04-19** (commit fbda124)
- [x] 02-08-PLAN.md -- gemini.ts adapter + 7 tests (ADPT-08) — **completed 2026-04-19** (commit 72c6f69)
- [x] 02-09-PLAN.md -- registry swap + HealthStatus.warning amendment + adapter-registry.test.ts (ADPT-09) — **completed 2026-04-19** (commits db1e65d + a2f0563 + fc2b84a + 78eaaf7)
- [x] 02-10-PLAN.md -- exit gate + manual smoke tests + ROADMAP/STATE updates (ADPT-03..09 + SAFE-02 code complete; 2 manual smokes pending user) — **code complete 2026-04-19** (commit 0331f69 contracts + this closeout commit; automated gate green: typecheck + vitest 104/104 + supervisor 24/24 + frozen-surface diff 0 lines vs PHASE2_BASE 0ec59df)

### Phase 3: Editor
**Goal**: Deliver a `/editor` route where a user can fill a form, hit Save, and have a valid, secret-free, collision-checked routine bundle written to `routines-<runtime>/<slug>/` with autosave protecting against lost work.
**Depends on**: Phase 2
**Requirements**: EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05
**Success Criteria** (what must be TRUE):
  1. A user can open `/editor`, fill in name + prompt + runtime + cron + reversibility + budget, see an immediate `cronstrue` human-readable preview of the cron, and click Save to get `config.json` + `prompt.md` atomically written to `routines-<runtime>/<slug>/`; unavailable runtimes are dimmed with a fix-instructions tooltip.
  2. Pasting a Stripe `sk_live_` key, a GitHub `ghp_` token, a 40-char hex secret, or any gitleaks-matched pattern into the prompt blocks Save with a specific error pointing to the `${VAR}` pattern in AUTHORING.md; the disk is never touched on a blocked save.
  3. A user can spend 10 minutes editing, accidentally close the tab or click "Routines" in the sidebar, and return to find their draft intact via localStorage + in-app navigation intercept; no keystrokes lost.
  4. Attempting to save a routine with slug `../../../evil`, `Has Spaces`, `UPPERCASE`, or a `(runtime, slug)` pair that already exists anywhere under `routines-*/` is rejected with a clear message and no partial writes.
  5. Password managers (1Password, LastPass), browser autofill, browser spellcheck, and autocorrect cannot silently mutate the prompt textarea; inputs announce their opt-out via `autocomplete="off"`, `data-1p-ignore`, and siblings.
**Plans**: 9 plans

Plans:
- [x] 03-01-PLAN.md -- deps install + vitest jsdom config + RoutineBundleInput zod schema + 12-block accept/reject matrix (EDIT-02, EDIT-04) — **completed 2026-04-19** (commits 104547f + 8286db4; suite 137 → 161 green)
- [x] 03-02-PLAN.md -- secret-patterns.ts + scanForSecrets pure utility + 18-block pattern test matrix (EDIT-02) — **completed 2026-04-19** (commits 64fb6ec + 891e2f3; suite 161 → 179 green)
- [x] 03-03-PLAN.md -- bundles.ts read-side directory enumeration (listBundles/hasBundle/hasBundleAnyRuntime/readBundle) + 18 tests (EDIT-04) — **completed 2026-04-19** (commit 509adb0; suite 179 → 197 green)
- [x] 03-04-PLAN.md -- atomic-write.ts directory-swap helper + 8-scenario test matrix (EDIT-02) — **completed 2026-04-19** (commit 96690b0; suite 197 → 205 green)
- [x] 03-05-PLAN.md -- saveRoutine + checkSlugAvailability Server Actions + 16-block E2E tests (EDIT-02, EDIT-04) — **completed 2026-04-19** (commits 5505e32 + 70cc247; suite 205 → 221 green)
- [x] 03-06-PLAN.md -- /editor page.tsx shell + editor-client stub + RuntimeRadioGrid + CronPreview + 10 jsdom .test.tsx blocks (EDIT-01 partial — 2/4 VALIDATION rows green) — **completed 2026-04-19** (commits f343478 + 92e8313 + f302d3d; suite 221 → 231 green; first jsdom tests in repo; vitest esbuild jsx=automatic)
- [x] 03-07-PLAN.md -- SecretScanPanel + DraftRecoveryBanner + PreviewPanel presentational subcomponents + 6 jsdom test blocks (EDIT-03 partial — 1/4 VALIDATION rows green: banner visibility) — **completed 2026-04-19** (commits 674d86e + 9b14e09 + 9742c56 + 555e9de; suite 231 → 237 green; `/editor` route still 640 B / 141 kB; Node 25 + jsdom localStorage polyfill installed inline in test)
- [x] 03-08-PLAN.md -- EditorClient full state machine: useActionState wiring against saveRoutine + 500ms autosave + slug auto-derive + beforeunload intercept + autofill opt-out + 13-block integration tests (EDIT-01 + EDIT-03 + EDIT-05 code-complete; only 4 manual-only verifications remain) — **completed 2026-04-19** (commit 5e7d125; suite 237 → 250 green; `/editor` route 640 B → 13.2 kB / 141 kB → 154 kB; one Rule-3 auto-fix: preview-panel.tsx dropped `@/lib/runtime-adapters/slug` import that pulled node:path + node:os into the client bundle)
- [x] 03-09-PLAN.md -- phase exit gate: frozen-surface diff + test suite green + 03-VALIDATION flip + ROADMAP/STATE update — **completed 2026-04-19** (typecheck exit 0; 250/250 dashboard tests green; supervisor bash harness 24/24; frozen-surface 0-line diff vs PHASE3_BASE `104547f` across 20 v0.1 + Phase 2 paths)

### Phase 4: Deploy
**Goal**: Wire adapters and editor together so Deploy, Run-now, enable/disable, Save-to-repo, and health badges are one-click actions on the dashboard, with a state-machine deploy that auto-rolls-back on partial failure.
**Depends on**: Phase 3
**Requirements**: DEPL-01, DEPL-02, DEPL-03, DEPL-04, DEPL-05, REPO-01, HLTH-01
**Success Criteria** (what must be TRUE):
  1. A user can deploy a Codex routine via the Deploy button and watch the UI progress through `planning -> writing -> loading -> verified`; on success `launchctl print gui/$UID/com.sleepwalker.codex.<slug>` returns a live state within 3 seconds; on any step failure every artifact is rolled back (`launchctl bootout` + delete plist + delete state) with zero orphaned files on disk or in launchd.
  2. The Run-now button fires an immediate run for each of the four runtimes using the runtime-appropriate mechanism (Claude Routines `/fire`, Claude Desktop `claude -p`, Codex/Gemini supervisor) and the resulting run appears in the Morning Queue with the same shape as a scheduled run.
  3. Routine cards display Draft / Deployed / Drift status reading from `~/.sleepwalker/deploys/<slug>.state.json` and mtime comparison; per-routine enable/disable toggles call `bootout`/`bootstrap` and persist state in `config.json`.
  4. Clicking Save-to-repo stages only the exact files under `routines-<runtime>/<slug>/`, flock-guards the git index via `~/.sleepwalker/git.lock`, shows the user a `git diff --stat` preview before confirmation, never auto-pushes, and never sweeps up the user's unrelated uncommitted work.
  5. The dashboard landing page displays four runtime health badges (Claude Routines / Claude Desktop / Codex / Gemini) following the `brew doctor` pattern — green when ready, grey with a linked fix-instruction when not.
**Plans**: 9 plans
**UI hint**: yes

Plans:
- [x] 04-01-PLAN.md -- deps (simple-git + proper-lockfile) + deploy-state.ts atomic I/O + drift math + 11 it() blocks (DEPL-01 + DEPL-03 primitives) — **completed 2026-04-20** (commits `8707433` chore deps + scaffold + `e3526c1` feat impl + 11 it() blocks; suite 272 → 283 green; 6/36 VALIDATION rows green — rows 1 + 2 + 3 + 9 + 10 + 12)
- [x] 04-02-PLAN.md -- save-to-repo.ts simple-git + proper-lockfile wrapper + never-push + real-git-repo tests (REPO-01) — **completed 2026-04-20** (commits `55740f8` feat module + `7279030` test matrix; suite 283 → 291 green; 13/36 VALIDATION rows green — rows 21-27 all green; never-push + never-sweep invariants grep-verifiable)
- [x] 04-03-PLAN.md -- /api/health/all Route Handler with 2s per-adapter timeout + Promise.allSettled (HLTH-01 server) — **completed 2026-04-20** (commits `22b3740` feat Route Handler + `de000a6` test matrix; suite 291 → 297 green; 16/36 VALIDATION rows green — rows 28+29+30 flipped 4-03-02; Wave 0 COMPLETE)
- [x] 04-04-PLAN.md -- routines/actions.ts 4 deploy-family Server Actions (deploy state machine + rollback + runNow + setEnabled) (DEPL-01..05) — **completed 2026-04-20** (commits `c5a9c75` feat amend DeployState warning field + `d06d22b` feat routines/actions.ts 860 lines with deployRoutine state machine + 10s Promise.race rollback timeout + getDeployState pass-through + runNowRoutine dispatch + setRoutineEnabled with bootstrap/bootout + per-runtime persistEnabledFlag; `8047e2e` test deploy-routine-action 9 blocks covering VALIDATION rows 1+5+6+7+8; `85cd378` test run-now-action 6 + set-enabled-action 6 blocks covering rows 13-20; suite 297 → 318 green across 34 files; 29/36 VALIDATION rows green; DEPL-01/02/04/05 Server Action surfaces code-complete)
- [x] 04-05-PLAN.md -- routines/actions.ts 3 save-to-repo Server Action wrappers + integration round-trip (REPO-01) — **completed 2026-04-20** (commits `1ae5398` feat append previewSaveToRepoAction + commitSaveToRepoAction + releaseSaveLockAction as pure pass-throughs to @/lib/save-to-repo; `659ef16` test save-to-repo-action 4 it() blocks real-git integration matrix against mkdtempSync repo — preview ok+lockToken, preview→commit round-trip, preview→release→preview re-allows, preview→preview returns lock-busy; suite 318 → 322 green across 35 files; never-push / never-sweep invariants propagate unchanged from lib → action layer)
- [x] 04-06-PLAN.md -- HealthBadgeRow + HealthBadge client components with 60s cache + focus refetch + manual refresh (HLTH-01 client) — **completed 2026-04-20** (commits `e3492ea` feat HealthBadge presentational pill with 4 state variants (green/amber/grey/loading) + `df2c279` feat HealthBadgeRow `"use client"` with three-layer cache (sessionStorage TTL 60_000ms + window-focus stale refetch + per-badge manual refresh) consuming /api/health/all + `d016d98` test jsdom matrix 5 it() blocks matching 04-VALIDATION.md rows 31-35 anchor filters verbatim; suite 322 → 327 green across 36 files; 34/36 VALIDATION rows green; HLTH-01 code-complete end-to-end — landing page mount deferred to 04-09)
- [x] 04-07-PLAN.md -- DeployProgressDrawer + DeployStepPill + StatusPill + RunNowButton with framer-motion + 500ms polling (DEPL-01..04) — **completed 2026-04-20** (commits `69836bc` feat DeployStepPill + StatusPill + RunNowButton with UI-SPEC-locked copy + 800ms busy window + per-runtime toast table; `c585448` feat DeployProgressDrawer with framer-motion slide-in spring damping=25 stiffness=200 + 500ms setInterval polling + terminal-state teardown + role=alert rollback banner + Q1 warning pill-amber surface + Esc-only-when-terminal keyboard guard + focus-close-on-open + retry-resets-invokedRef; `b22444c` test jsdom matrix 5 it() blocks mocking `@/app/routines/actions` via vi.mock + dynamic component import: stops polling on terminal state (VALIDATION row 4) / rollback banner with role=alert / Close + Run now footer on succeeded / Dismiss + Retry deploy footer on rolled-back / warning surface on claude-desktop succeeded; suite 327 → 332 green across 37 files; 35/36 VALIDATION rows green — row 4 flipped `4-07-03 ✅ green`; `/routines` route bundle stays at 2.14 kB because Plan 04-09 wires the import into routines-client)
- [x] 04-08-PLAN.md -- SaveToRepoModal two-stage Review->Confirm + DiffStatPanel + ConfirmDialog (REPO-01 + DEPL-05) — **completed 2026-04-20** (commits `b09ab93` feat DiffStatPanel + ConfirmDialog presentational components (DiffStatPanel `{files, totals}` consumer with UI-SPEC-locked empty-state + non-empty heading + per-file text-signal-green/text-signal-red counts and Unicode MINUS SIGN U+2212 verbatim; ConfirmDialog shared role=dialog aria-modal with AnimatePresence fade+scale + focus-on-cancel + Esc close + backdrop click + btn-ghost/btn-danger CTAs + panel-raised max-w-md centered via flex overlay with pointer-events-none/auto opt-in — placed at app/_components/ for 04-09 DisableToggle cross-segment import) + `feadcd6` feat SaveToRepoModal two-stage Review->Confirm with flock lifecycle + Cmd/Ctrl+Enter submit (555 lines across 3 sub-components; state {stage, preview, message, committing, confirmDiscardOpen, committed}; useEffect on [open, runtime, slug] kicks off previewSaveToRepoAction with invokedRef Strict Mode guard; Stage 1 branches preview===null→Loader2 spin, preview.ok→DiffStatPanel, preview.kind='lock-busy'→amber role=alert with UI-SPEC line 214 verbatim, preview.kind='no-changes'→empty DiffStatPanel, preview.kind='git-error'→red role=alert; Continue disabled when !preview.ok or totals.filesChanged===0; Cancel branches preview.ok→ConfirmDialog Discard confirm → releaseSaveLockAction+onClose, !preview.ok→direct onClose; Stage 2 Confirm renders `Commit message` heading + one-line diff summary + MESSAGE label + textarea rows=3 with INPUT_OPT_OUT 8-attr bag identical to editor-client.tsx + placeholder + onKeyDown (e.metaKey||e.ctrlKey)&&e.key==='Enter' → handleCommit + helper + never-push subtitle UI-SPEC line 216 verbatim + Back/Commit CTAs; handleCommit invokes commitSaveToRepoAction — on ok: setCommitted(true) + green 6s toast `Committed ${shortSha} — ${message.split('\n')[0]}` + onClose; on !ok: red 8s toast + stay; safe-unmount release effect releases flock when preview.ok && !committed; focus mgmt across stages; Esc routing); suite stays 332/332 green across 37 files — unchanged by plan design (Plan 04-05 real-git matrix + Plan 04-02 lib matrix already cover save-to-repo boundary end-to-end; jsdom mocking of save-to-repo.ts module-scoped flock registry infeasible without heavy scaffolding; VALIDATION.md §Manual-Only row 4 two-tab lock-busy resolves by inspection); typecheck exit 0; `pnpm build` `/routines` 2.14 kB / 143 kB and `/editor` 13.2 kB / 154 kB UNCHANGED — Plan 04-09 wires SaveToRepoModal trigger into routines-client + ConfirmDialog-backed DisableToggle; client-bundle safety check passes; REPO-01 UI surface code-complete at component level)
- [x] 04-09-PLAN.md -- route integration (routines/page + routines-client + landing page) + RoutineActionBar + Phase 4 exit gate (all 7 requirements) — **completed 2026-04-20** (commits `1f1feb6` feat listRoutinesAsync + RoutineActionBar + StatusPill wiring + `71f920d` feat HealthBadgeRow landing mount + routines-page integration test (4 it() blocks); suite 332 → 336 green; typecheck exit 0; supervisor harness 28/0; frozen-surface diff 0 lines across 24 enumerated v0.1 + Phase 2/3 paths vs PHASE4_BASE `8707433^`)

### Phase 5: Queue
**Goal**: Extend the Morning Queue and audit surface so Codex and Gemini runs produce normalized, ANSI-stripped, flock-protected JSONL that flows through every existing v0.1 consumer without code changes.
**Depends on**: Phase 4
**Requirements**: QUEU-01, QUEU-02, QUEU-03, QUEU-04, SAFE-01
**Success Criteria** (what must be TRUE):
  1. A user sees Codex and Gemini entries in the same Morning Queue as local and cloud, with distinct source pills (pure CSS, no new dependency), identical approve/reject affordances, and the existing v0.1 aggregator logic untouched.
  2. Codex/Gemini audit entries contain `runtime`, `fleet` as `<runtime>/<slug>`, ANSI-stripped content (no `\x1b[` sequences, no `[32m` prefixes), ISO 8601 timestamps, and char-count budget info; `jq .` parses every line.
  3. Four concurrent routines (one per runtime) writing to `audit.jsonl` simultaneously produce zero corrupted or interleaved JSON lines — `flock` on the write path eliminates the race v0.1 CONCERNS.md flagged.
  4. A Codex or Gemini routine that blows past its character budget is SIGTERMed by the supervisor within one budget interval; the audit log shows a `cli-budget-exceeded` event labeled "approximate" (never "tokens"); stdout is captured up to the cutoff.
**Plans**:
- [x] 05-01-PLAN.md — widen QueueSource + QueueStatus unions in dashboard/lib/queue.ts for codex/gemini + complete/failed terminal states (QUEU-01 foundation) — **completed 2026-04-20** (single atomic commit `a545f0b` feat widen QueueSource to "local" | "cloud" | "codex" | "gemini" and QueueStatus to add "complete" | "failed" supervisor-run terminal states; strictly additive with zero consumer-side edits required — RESEARCH §3.2 grep-audit prediction validated by typecheck exit 0; 3 new it() blocks in queue.test.ts asserting codex/gemini/complete/failed round-trip through appendQueueEntry + readLocalQueue; dashboard suite 336 → 339 green across 38 files; QUEU-01 type surface code-complete at lib boundary but requirement remains Partial until 05-03 readSupervisorRuns + 05-07 UI branch land)
- [x] 05-02-PLAN.md — add `.pill-codex` + `.pill-gemini` CSS classes to dashboard/app/globals.css (QUEU-02) — **completed 2026-04-20** (single atomic commit `548d432` `feat(05-02)` add 2 Tailwind utility classes to globals.css @layer components block at lines 78-79 — `.pill-codex` uses `bg-aurora-500/10 text-aurora-400 border-aurora-500/30` (deeper blue than cloud's aurora-400; ~7.8:1 WCAG AA on ink-900 per RESEARCH §4.3), `.pill-gemini` uses `bg-dawn-400/10 text-dawn-400 border-dawn-400/30` (warm highlight; ~11.0:1 WCAG AA); pure CSS, zero palette edits — aurora.500 + dawn.400 pre-exist in tailwind.config.js; build + typecheck + test all exit 0; dashboard suite stays 339/339 green — no new test files by plan design, Wave 3 Plan 05-07 owns the jsdom class-attachment assertion; QUEU-02 CSS producer surface code-complete; Wave 0 COMPLETE)
- [x] 05-03-PLAN.md — readSupervisorRuns normalized reader for codex/gemini supervisor audit entries (QUEU-03) — **completed 2026-04-21** (two atomic commits `3c81b4f` `feat(05-03): add readSupervisorRuns + 3-source aggregator merge` extending `dashboard/lib/queue-aggregator.ts` 49→208 lines with `SupervisorAuditEntry` interface mirroring `bin/sleepwalker-run-cli::audit_emit` contract verbatim + `TERMINAL_EVENTS`/`SUPERVISOR_RUNTIMES` allowlist sets + `supervisorRunToQueueEntry` mapper with deterministic `q_sup_${runtime}_${fleetMunged}_${tsCompact}_${event}` id + eager source:runtime tag + kind:"supervisor-run" discriminator + 4-way status switch (completed→complete, failed|budget_exceeded→failed, deferred→rejected) + payload carrying all 8 upstream extras, exported `readSupervisorRuns(): QueueEntry[]` reader with fs.existsSync guard + try/catch JSON.parse per line + defensive 4-field shape check filtering out v0.1 hook entries + 24h cutoff via `Date.now() - 24*60*60*1000`, `aggregateQueue()` extended to 3-source merge `[...local, ...cloud, ...supervisor]`, `AggregatedQueue` gains additive `supervisorCount` field; new file `dashboard/tests/supervisor-runs.test.ts` 204 lines 12 it() blocks covering RESEARCH §6.1 matrix verbatim — missing/empty file, v0.1-hook-drop, runtime filter, started filter, 4 event-to-status mappings, 24h cutoff, malformed-line skip, deterministic-id regex — plus `a3e85e5` `test(05-03): assert aggregateQueue merges all 4 sources` appending 1 new integration block to `dashboard/tests/queue-aggregator.test.ts` asserting localCount=1, cloudCount=1, supervisorCount=2, all 4 source literals surface, and both supervisor entries carry kind:"supervisor-run"; dashboard suite 339 → 352 green across 38 → 39 files (+13); typecheck exit 0; QUEU-03 byte-identical supervisor invariant verified via `git diff HEAD~2 HEAD -- bin/sleepwalker-run-cli` returning 0 lines — Phase 2 write-side source untouched; PHASE5_BASE sentinel file established at `dashboard/tests/supervisor-runs.test.ts` so Plan 05-08 frozen-surface gate resolves PHASE5_BASE=3c81b4f^ = 37ab5d9; zero Rule 1/2/3 auto-fixes; one additive correctness refinement to tsCompact regex for millisecond timestamps documented in SUMMARY §Deviations)
- [x] 05-04-PLAN.md — flock-wrap supervisor audit_emit + concurrency tests (QUEU-04 supervisor side) — **completed 2026-04-21** (single atomic commit `c139354` `feat(05-04): flock-wrap supervisor audit_emit + concurrency tests` — `bin/sleepwalker-run-cli` audit_emit function wrapped in FD-form flock `-w 5 -x 200` on sidecar `${HOME}/.sleepwalker/audit.jsonl.lock` with `|| true` graceful fallthrough on timeout; Phase 2 JSON shape byte-identical (both printf branches preserved verbatim); `hooks/tests/supervisor-tests.sh` +105 lines with Scenario 8 (4-way codex+gemini concurrent zero-corruption: 8 lines, jq -e . per-line round-trip, 4 fleets observed) + Scenario 9 (6s holder blocks 5s waiter → `|| true` fallthrough preserves both events + exit 0); supervisor-tests 28 → 30 scenarios, 36/36 assertions green; dashboard suite stays 352/352 green across 39 files — no TS touched; typecheck exit 0; shared sidecar lock path with Plan 05-05 hook writer closes cross-file mutex invariant; T-05-01 + T-05-03 mitigated; T-05-02 + T-05-04 accepted per RESEARCH §1.5; T-05-05 deferred to Plan 05-06 install preflight; plan executed exactly as written — zero Rule 1/2/3 auto-fixes, zero architectural deviations, zero auth gates)
- [x] 05-05-PLAN.md — flock wrap on audit.jsonl write path (QUEU-04) + hooks/sleepwalker-audit-log.sh — **completed 2026-04-21** (commit `13cd12b` `feat(05-05): flock-wrap hook audit-log + concurrency test` — hook append wrapped in FD-form flock `-w 5 -x 200` on shared sidecar `${HOME}/.sleepwalker/audit.jsonl.lock` (byte-identical to 05-04 — both writers now serialize on one kernel mutex); strict `set -euo pipefail` drop-the-entry policy on timeout per CONTEXT.md hook/supervisor failure-mode asymmetry; `hooks/tests/run-tests.sh` concurrent-audit scenario added with 4-way writer race + jq round-trip; run-tests 26 → 29 PASS; supervisor-tests still 36/36; dashboard suite 352/352; typecheck exit 0; QUEU-04 both writers now mutexed)
- [x] 05-06-PLAN.md — install.sh flock preflight check (QUEU-04 install-boundary seal; closes T-05-05) — **completed 2026-04-20** (single atomic commit `71bfdcc` `feat(05-06): add flock preflight check to install.sh` — `install.sh` lines 32-37 add `if ! command -v flock >/dev/null 2>&1; then echo ERROR: flock is required but not installed. Install with: brew install flock; echo ... 2-line why-context re: audit.jsonl serialization across supervisor + PostToolUse hook; exit 1; fi` block immediately after the v0.1 jq preflight and before state-dir init; idiom mirrors jq check byte-for-byte in shape for grep-verifiable consistency; purely additive diff — 7 insertions / 0 deletions; `bash -n install.sh` exit 0; supervisor-tests 36/36 + run-tests 29/29 zero regression; v0.1 frozen-surface preserved byte-identically everywhere else — `grep -c "Copying hooks to|Wiring hooks into|Initialize state directory" install.sh` = 3; QUEU-04 3-layer defense complete: install preflight + supervisor flock + hook flock all reference flock on one shared sidecar `${HOME}/.sleepwalker/audit.jsonl.lock`)
- [x] 05-07-PLAN.md — queue-client.tsx UI source branch for codex/gemini pills + SAFE-01 UI copy sweep — **completed 2026-04-20** (two atomic commits `8eebb80` `fix(05-07)` flips `routines-client.tsx:62` from `budget: {n} tokens` to `budget: {n} chars (approximate)` + adds `<p>Approximate character cap. Tokens vary by ±40% depending on output format.</p>` helper below editor-client.tsx budget input; `373b342` `feat(05-07)` adds exported SourceIcon + SourcePill inline helpers to queue-client.tsx driving a 4-way source discriminator (codex → Terminal icon + `.pill-codex`, gemini → Sparkles + `.pill-gemini`, cloud → Cloud + `.pill-aurora`, local → HardDrive + signal-green inline), supervisor-run ActionDetail branch with labeled Event/Preview/Reason/Exit code rows + `Stopped at {chars} chars (budget: {limit}, approximate)` copy for budget_exceeded, 3-way footer action row (terminal `/audit` deep-link for supervisor-run, Dismiss+OpenPR for cloud, Reject+Approve for local), RecentList statusPillClass widening to handle QueueStatus complete/failed, plus new dashboard/tests/queue-client.test.tsx with 6 jsdom it() blocks; dashboard suite 352 → 358 green across 40 files; typecheck + build exit 0; SAFE-01 negative invariant `grep -rn 'budget.*tokens|tokens.*budget' dashboard/app/` empty)
- [x] 05-08-PLAN.md — Phase 5 exit gate + VALIDATION flip + frozen-surface diff — **completed 2026-04-21** (docs-only phase seal; automated 4-step gate green: typecheck exit 0, dashboard suite 358/358 across 40 files in 10.62s, supervisor-tests 36/36 across 30 scenarios, run-tests 29/29; PHASE5_BASE resolved dynamically via sentinel file `dashboard/tests/supervisor-runs.test.ts` first-add commit parent = `3c81b4f^` = `37ab5d9` (Plan 05-02 seal); frozen-surface diff 0 lines across 31 enumerated v0.1 + Phase 2/3/4 paths with corrected hook filenames (`sleepwalker-defer-irreversible.sh` + `sleepwalker-budget-cap.sh` per plan-check note #2); 3 documented QUEU-04 exceptions audited additive-only — bin/sleepwalker-run-cli +22/-5, hooks/sleepwalker-audit-log.sh +13/-1, install.sh +7/-0 with JSON shape + install.sh signature preserved via grep invariants; shared-sidecar lock path byte-identical across supervisor + hook writers; 05-VALIDATION.md flipped to `status: approved 2026-04-21` with all 48 rows ✅ green; REQUIREMENTS.md QUEU-01..04 + SAFE-01 all flipped Complete (27/32 v1 requirements Complete); single `docs(05)` commit sealing Phase 5; next: `/gsd-plan-phase 6`)
**UI hint**: yes

### Phase 6: Polish
**Goal**: Ship OSS-quality docs, per-runtime templates, a diagnostics page, and a backward-compatibility integration gate so a second user on a different Mac can go from clone to first custom routine in under 10 minutes without surprising any v0.1 user.
**Depends on**: Phase 5
**Requirements**: DOCS-01, DOCS-02, DOCS-03, COMP-01, COMP-02
**Success Criteria** (what must be TRUE):
  1. A user who has just cloned the repo can follow `docs/AUTHORING.md` and have a scheduled custom routine running on any of the four runtimes in under 10 minutes, including the Mac-sleep caveat with `caffeinate`/`pmset` alternatives and a Troubleshooting section indexed by error message.
  2. `templates/routine-claude-routines.md`, `templates/routine-claude-desktop.md`, `templates/routine-codex.md`, and `templates/routine-gemini.md` each contain a commented frontmatter block plus a skeleton prompt; a user copies one, edits it, and has a syntactically valid bundle.
  3. The `/diagnostics` page reports macOS version, Homebrew prefix (arm64/x86_64 detected at runtime), each CLI's absolute path, active shell, and `~/Library/LaunchAgents/` writability in a copy-to-GitHub-issue format; a fresh Intel Mac running fish shell with macOS 14 loads the page without crashing.
  4. An end-to-end integration test verifies that all 14 v0.1 routines (6 local + 8 cloud) deploy, run, and surface in the Morning Queue without code changes; `install.sh` is re-run idempotently on a v0.1 install and produces a no-op upgrade; no hook script name, path, or JSONL schema field has changed.
  5. The v0.1 frozen surface (hook paths, `~/.sleepwalker/*.jsonl` schemas, `~/.claude/settings.json` wiring, `QueueEntry` fields, `[sleepwalker:<fleet>]` marker format, reversibility colors, policy names) is verified unchanged by a backward-compatibility test run in CI.
**Plans**: 7 plans

Plans:
- [x] 06-01-PLAN.md -- DOCS-02: 4 runtime templates + gray-matter/zod round-trip test (templates/routine-*.md + dashboard/tests/templates.test.ts) — **completed 2026-04-22** (commits ce78dc9 + a6c590b; 5/5 templates.test.ts green; typecheck exit 0; zero new deps; deferred-items.md documents 50 pre-existing failures from parallel-session main advancement as out-of-scope)
- [x] 06-02-PLAN.md -- DOCS-03: /diagnostics Server Component + lib + 2 tests (dashboard/app/diagnostics/ + dashboard/lib/diagnostics.ts) — **completed 2026-04-22** (single atomic commit `72f7b63` `feat(06-02): add /diagnostics page + 11 fail-soft probes + copy-issue-body button` — 6 files / +662 / -1: dashboard/lib/diagnostics.ts new 218 lines (gatherDiagnostics + formatAsIssueBody + Probe + DiagnosticsSnapshot exports; 11 probes via Promise.allSettled with per-probe `{timeout: 2000, maxBuffer: 64_000}`; CLI probes use `/bin/zsh -l -c` matching Phase 2 supervisor PATH resolution; explicit-allowlist formatter Pitfall 1 defense; negative invariant secret-pattern grep = 0); dashboard/app/diagnostics/page.tsx new 92 lines (async Server Component with `export const dynamic = "force-dynamic"`; 11 lunar-palette panel rows + Last checked eyebrow + optional gitSha panel + No-secrets transparency footer; calls formatAsIssueBody server-side and passes string as `<CopyIssueButton issueBody={...} />` prop); dashboard/app/diagnostics/diagnostics-client.tsx new 56 lines (`"use client"` island; navigator.clipboard.writeText on user gesture only — T-06-02-04 mitigation; Copy → Check icon swap; silent no-op on rejection); dashboard/app/layout.tsx +1 NavLink between /audit and /settings + Wrench lucide-react import addition; dashboard/tests/diagnostics.test.ts new 191 lines / 6 it() blocks Node env (vi.mock(node:child_process) with hoisted execMock.impl indirection — happy path + brew-missing fail-soft + LaunchAgents-missing fail-soft + probeShell direct + probeSleepwalkerState v0.2 + formatAsIssueBody allowlist canary); dashboard/tests/diagnostics-page.test.tsx new 76 lines / 4 it() blocks jsdom env (vi.mock(@/lib/diagnostics) — >=11 .panel rows + Copy button + Last checked eyebrow + No-secrets dual-surface). Gates green: pnpm test 373/373 across 43 files in 10.5s (363 → 373); typecheck exit 0; pnpm build exit 0 with `/diagnostics` route registered as `ƒ` dynamic at 1.53 kB / 142 kB First Load JS; zero new deps. 2 Rule 1 auto-fixes documented in 06-02-SUMMARY.md: (1) `<CopyIssueButton>` prop shape `{diag}` → `{issueBody: string}` to keep node:* schemes out of client bundle (same precedent as Plan 03-08 preview-panel.tsx); (2) jsdom test `getByText` → `getAllByText` for "No secrets rendered" dual-surface design)
- [x] 06-03-PLAN.md -- DOCS-01: docs/AUTHORING.md 7-section walkthrough + README link — **completed 2026-04-22** (single atomic commit `38b99ed` `docs(06-03): ship AUTHORING.md 7-section walkthrough + README link` — 3 files / +622 / -1: `docs/AUTHORING.md` new 602 lines with 7 locked-order H2 sections (1. Quick Start / 2. Author a Custom Routine / 3. The Four Runtimes / 4. Scheduling & Mac Sleep / 5. Safety Semantics / 6. Troubleshooting / 7. Going Further) + 14 H3 subsections + 15-row grep-friendly Troubleshooting table indexed by verbatim error string (exceeds plan minimum of 13) + Mac-sleep §4.2 `caffeinate` / `pmset schedule` / `launchctl print` triptych verbatim from live-Mac man pages + §3.1-3.4 cross-links to `templates/routine-<runtime>.md` from Plan 06-01; `README.md` +1/-1 single-line additive link between QUICKSTART.md and ROUTINES.md (no section rewrite); `.planning/phases/06-polish/deferred-items.md` appended Plan 06-03 closeout addendum documenting 1 pre-existing `tests/routines.test.ts > listRoutines returns the 6 local templates` failure from parallel-session `sleepwalker-daily-standup` bundle commit `58e8712` as out of scope per SCOPE BOUNDARY rule. SAFE-01 negative invariant `grep -iE 'budget.*tokens|tokens.*budget' docs/AUTHORING.md` = 0 hits; primary-verify bash pipeline `AUTHORING.md PRIMARY-VERIFY: PASS`; acceptance criteria replay green across all 15 predicates. `pnpm test` = 372 passed / 1 failed / 373 total (1 failure is the pre-existing parallel-session tests/routines.test.ts hardcoded-6-vs-7-on-disk issue — docs-only plan cannot regress or fix it). Zero new deps. 2 Rule 1 auto-fixes documented in 06-03-SUMMARY.md: (1) §2.2 + §5.3 SAFE-01 prose rewritten twice because initial negative-form sentences ("never as 'tokens'", "They are NOT tokens") kept "budget" and "tokens" co-occurring on the same line — the invariant grep is polarity-blind, so final phrasings use "char-count approximation" + "unit word" as deliberate euphemisms; (2) initial 550-line draft under 600-line floor AND §4.2 / §4.3 fenced bash blocks with embedded `# shell-comment` lines leaked into `grep -c '^# '` count (17 vs expected 1) — both solved with one refactor expanding compact blocks to multi-fenced-block form with prose between them + `####` sub-anchors for §4.2 Pattern A/B/C + 5-step ordered flow for §4.3; final line count = 602, final H1 count = 1. DOCS-01 requirement flipped from Pending → Complete (30/32 v1 requirements Complete))
- [ ] 06-04-PLAN.md -- COMP-01: tests/compat/v01-routines.sh + dashboard/tests/v01-queue-integration.test.ts
- [ ] 06-05-PLAN.md -- COMP-02: tests/compat/frozen-surface.sh permanent gate vs 998455b baseline
- [ ] 06-06-PLAN.md -- CI: .github/workflows/ci.yml macos-14 runner with 6-step verification
- [ ] 06-07-PLAN.md -- (per on-disk plan; see .planning/phases/06-polish/06-07-PLAN.md)

## Progress

| Phase | Plans Complete | Status | Completed |
|-------|----------------|--------|-----------|
| 1. Foundation | 4/4 | Complete | 2026-04-18 |
| 2. Adapters | 10/10 | Code Complete (manual smokes pending) | 2026-04-19 |
| 3. Editor | 9/9 | Complete | 2026-04-19 |
| 4. Deploy | 9/9 | Complete | 2026-04-20 |
| 5. Queue | 8/8 | Complete | 2026-04-21 |
| 6. Polish | 3/7 | In progress | - |

---
*Roadmap created: 2026-04-18*
*Last updated: 2026-04-22 after Phase 6 Plan 03 execution (DOCS-01 `docs/AUTHORING.md` 602-line 7-section OSS authoring walkthrough + 15-row grep-friendly Troubleshooting table + Mac-sleep §4.2 `caffeinate`/`pmset`/`launchctl print` triptych + README.md single-line additive link sealed. **3/7 Phase 6 plans complete (42.9%); 30/32 v1 requirements Complete.** Single atomic commit `38b99ed` `docs(06-03): ship AUTHORING.md 7-section walkthrough + README link` on main (3 files / +622 / -1: `docs/AUTHORING.md` new + `README.md` +1/-1 + `.planning/phases/06-polish/deferred-items.md` Plan 06-03 closeout addendum appended). 7 locked-order H2 sections in PLAN-fixed order: 1. Quick Start / 2. Author a Custom Routine / 3. The Four Runtimes / 4. Scheduling & Mac Sleep / 5. Safety Semantics / 6. Troubleshooting / 7. Going Further. 14 H3 subsections. 15-row Troubleshooting table indexed by verbatim error string (exceeds plan minimum of 13) — every error from install.sh preflight / supervisor audit.jsonl / editor zod rejection / adapter healthCheck is reproduced literally (`ERROR: jq is required but not installed` / `ERROR: flock is required but not installed` / `"event":"failed","preview":"..Operation not permitted.."` / `"event":"failed","preview":"..Not inside a trusted directory and --skip-git-repo-check was not specified.."` / Claude Desktop routine never fires even after save / `"event":"budget_exceeded"` / `"event":"deferred","preview":"red+balanced"` / `Gemini quota project not configured. Set runtime_config.gemini_quota_project in config.json.` / `codex CLI not found on login-shell PATH` / `"event":"failed","preview":"bundle not found"` / `<integer>NaN</integer>` plutil reject / `OPENAI_API_KEY set but ~/.codex/auth.json present` / secret-scan `ghp_` + `sk_live_` / slug regex / slug cross-runtime collision / `cloud queue inactive · configure GitHub in Settings`). §4.2 Mac-sleep triptych documents all three patterns with verbatim commands from live-Mac `man caffeinate` + `man pmset`: Pattern A `caffeinate -i [&/-t 28800/-w $(pgrep...)]`, Pattern B `sudo pmset schedule wake "MM/DD/YY HH:MM:SS"` / `sudo pmset repeat wake MTWRFSU 03:00:00` / `pmset -g sched` / `sudo pmset schedule cancelall`, Pattern C `launchctl print gui/$UID/com.sleepwalker.codex.morning-brief` + 5-step debug flow through pending-jobs counter + working-directory TCC check + `plutil -p` / `plutil -lint`. §3.1-3.4 cross-links each `templates/routine-<runtime>.md` from Plan 06-01 — one per runtime subsection. §1 Quick Start references `/diagnostics` as the prereq-check landing. §6 Troubleshooting deep-links rows back to §4.2 / §4.3 / §5 Safety Semantics. SAFE-01 negative invariant `grep -iE 'budget.*tokens|tokens.*budget' docs/AUTHORING.md` = 0 hits after 2 prose rewrites (Rule 1 auto-fixes #1 documented in 06-03-SUMMARY.md — initial negative-form sentences like "never as 'tokens'" and "They are NOT tokens" still co-occurred "budget" + "tokens" on one line; polarity-blind grep defeated by rewriting to "char-count approximation" + "unit word" euphemisms). Rule 1 auto-fix #2 expanded initial 550-line draft to 602 lines AND cleared 17 `^# ` hits down to 1 (H1-uniqueness acceptance criterion) via §4.2 + §4.3 refactor from single-fenced-blocks-with-comments to multi-fenced-blocks-with-prose + `####` sub-anchors for Pattern A/B/C + 5-step ordered flow for §4.3. Primary-verify bash pipeline `AUTHORING.md PRIMARY-VERIFY: PASS` end-to-end. Acceptance criteria replay all green: 602 lines (≥600 ≤1100), 7 H2 headings (exact PLAN order), 18 H3 subsections (≥10), 1 `^# ` line, 15 Troubleshooting data rows (≥13), 4 §3 H3 subsections, 7 templates/routine- cross-links, 2 QUICKSTART.md references, 1 README.md link, all 4 runtime strings present (claude-routines=5, claude-desktop=4, codex=30, gemini=17), 8 literal error strings. README.md amendment is a single-line additive insertion between QUICKSTART.md and ROUTINES.md (no section rewrite, no link reorder). `pnpm test` = 372 passed / 1 failed / 373 total — the 1 failure is the pre-existing `tests/routines.test.ts > listRoutines returns the 6 local templates` from parallel-session `sleepwalker-daily-standup` bundle commit `58e8712` (hardcoded 6 vs on-disk 7); Plan 06-03 is docs-only and cannot regress or fix it per SCOPE BOUNDARY rule — logged in `.planning/phases/06-polish/deferred-items.md` addendum for Phase 6 exit-gate reconciliation. Pre-existing untracked files (`CLAUDE.md` + 2 screenshot PNGs) preserved untouched via explicit per-file `git add docs/AUTHORING.md README.md .planning/phases/06-polish/deferred-items.md` staging — zero scope bleed; `git diff --diff-filter=D --name-only HEAD~1 HEAD` returns empty (no deletions). DOCS-01 requirement flipped from Pending → Complete (30/32 v1 requirements Complete; 2 remain: COMP-01 + COMP-02). Plan executed otherwise exactly as written — zero Rule 2 missing-critical auto-fixes, zero Rule 4 architectural decisions, zero auth gates. 06-03-SUMMARY.md shipped at `.planning/phases/06-polish/06-03-SUMMARY.md` with Self-Check: PASSED footer. Downstream unlocked: Plan 06-04 (COMP-01) + 06-05 (COMP-02) + 06-06 (CI) — none have AUTHORING.md dependencies. Next: `/gsd-execute-phase 6` → Plan 06-04 (COMP-01 bash + TS integration tests).)*

*Previous update: 2026-04-22 after Phase 6 Plan 02 execution (DOCS-03 `/diagnostics` Server Component + 11 fail-soft execFile probes via Promise.allSettled + Copy-as-GitHub-issue-body button sealed. 2/7 Phase 6 plans complete (28.6%); 29/32 v1 requirements Complete. Single atomic commit `72f7b63` `feat(06-02): add /diagnostics page + 11 fail-soft probes + copy-issue-body button` on main (6 files / +662 / -1) ships dashboard/lib/diagnostics.ts (218 lines: gatherDiagnostics + formatAsIssueBody + Probe + DiagnosticsSnapshot exports; 11 probes via Promise.allSettled with per-probe `{timeout: 2000, maxBuffer: 64_000}`; CLI probes use `/bin/zsh -l -c` matching Phase 2 supervisor PATH resolution; explicit-allowlist formatter Pitfall 1 defense; negative invariant `grep -cE 'github-token|bearer|credentials|sk_live|ghp_|process\.env\.[A-Z_]*TOKEN' dashboard/lib/diagnostics.ts` = 0) + dashboard/app/diagnostics/page.tsx (92 lines: async Server Component with `dynamic="force-dynamic"`; 11 lunar-palette panel rows + Last checked eyebrow + optional gitSha panel + No-secrets transparency footer; calls formatAsIssueBody server-side and passes string as `<CopyIssueButton issueBody={...} />` prop) + dashboard/app/diagnostics/diagnostics-client.tsx (56 lines: `"use client"` island; navigator.clipboard.writeText on user gesture only; Copy → Check icon swap; silent no-op on rejection) + dashboard/app/layout.tsx (+1 NavLink between /audit and /settings + Wrench icon import) + dashboard/tests/diagnostics.test.ts (191 lines / 6 it() blocks Node env covering happy path + brew-missing fail-soft + LaunchAgents-missing fail-soft + probeShell direct + probeSleepwalkerState v0.2 + formatAsIssueBody allowlist canary) + dashboard/tests/diagnostics-page.test.tsx (76 lines / 4 it() blocks jsdom env covering >=11 .panel rows + Copy button + Last checked eyebrow + No-secrets transparency dual-surface). Gates green: `pnpm test` 373/373 across 43 files in 10.5s (363 baseline + 10 new); `pnpm run typecheck` exit 0; `pnpm run build` exit 0 with `/diagnostics` route registered as `ƒ` (dynamic, server-rendered on demand) at 1.53 kB / 142 kB First Load JS; zero new deps. 2 Rule 1 auto-fixes documented in 06-02-SUMMARY.md: (1) restructured `<CopyIssueButton>` prop shape from `{diag: DiagnosticsSnapshot}` to `{issueBody: string}` after initial build failed with `UnhandledSchemeError: Reading from "node:util" is not handled by plugins` — same precedent as Plan 03-08 preview-panel.tsx; page.tsx now calls `formatAsIssueBody(snapshot)` server-side and passes the string down so the client island has zero `@/lib/diagnostics` imports while the formatter still owns the explicit-allowlist Pitfall 1 defense; (2) jsdom test "No secrets rendered" assertion switched from `getByText` to `getAllByText` after first run failed `Found multiple elements with the text` — the page intentionally renders the phrase in BOTH PageHeader subtitle (first-impression reassurance) and dedicated transparency footer (post-button explicit statement); both surfaces required by must_haves.truths. DOCS-03 requirement flipped Pending → Complete (29/32 v1 requirements Complete). Plan executed otherwise exactly as written — zero Rule 2 missing-critical auto-fixes, zero Rule 4 architectural decisions, zero auth gates. Pre-existing untracked files (`CLAUDE.md` + 2 screenshot PNGs) preserved untouched via explicit per-file `git add` staging — zero scope bleed. Downstream unlocked: Plan 06-03 (AUTHORING.md §1 Quick Start can reference `/diagnostics` as the prereq-check link; §6 Troubleshooting can deep-link specific error rows to diagnostics output) + Plan 06-06 (CI workflow picks up the new diagnostics test files automatically via existing `pnpm test` invocation). Next: `/gsd-execute-phase 6` → Plan 06-03 (DOCS-01 AUTHORING.md). Note: pre-existing 50-test failure set documented during Plan 06-01 in `.planning/phases/06-polish/deferred-items.md` is no longer present at HEAD `72f7b63` — full suite 373/373 green; the parallel-session-introduced failures were resolved upstream before Plan 06-02 execution.)*

*Previous update: 2026-04-22 after Phase 6 Plan 01 execution (DOCS-02 four runtime templates + gray-matter+zod round-trip test sealed. 1/7 Phase 6 plans complete. Two atomic commits on main: `ce78dc9` `feat(06-01): add 4 runtime templates + gray-matter+zod round-trip test` (6 files / +361 / -0 — `templates/routine-claude-routines.md` + `templates/routine-claude-desktop.md` + `templates/routine-codex.md` + `templates/routine-gemini.md` authored with lowercase zod frontmatter keys + v0.2 `[sleepwalker:<runtime>/<slug>]` fleet marker + per-runtime example prompts (Morning Brief / Inbox Triage / Dependency Update Scan / Design Doc Drift Review); `dashboard/tests/templates.test.ts` with 5 lexical it() blocks (4 per-runtime parameterized via assertRoundTrip helper + 1 negative invariant on `description` key) round-trips each template through gray-matter + RoutineBundleInput.safeParse; `templates/.gitkeep` removed as redundant); `a6c590b` `docs(06-01): activity log -- 4 runtime templates + round-trip test` (1 file / +25 — CLAUDE.md §Activity Log entry). Acceptance verified: each template has runtime=1 marker=1 name=1 slug=1 schedule=1 reversibility=1 budget=1 description=0; claude-desktop manual-add/Q1 comment count=2; gemini_quota_project note count=1; test lexical it()=5, safeParse=2, import matter=1; `pnpm test tests/templates.test.ts` 5/5 green; `pnpm run typecheck` exit 0; zero new deps (gray-matter 4.0.3 + zod already installed per Phase 3). Scope discipline: during execution, parallel session(s) advanced main from `1408625` to `82b9c2f` (+7 commits including `cfcf0ab refactor(editor): drop existingSlugs prop`, `e38ffd4 fix(drawer): render deploy-progress-drawer through createPortal`, `82b9c2f test(supervisor-runs): use nowIso helper`); those changes introduced 50 pre-existing test failures across `bundles.test.ts` + `deploy-routine-action.test.ts` + `routines-page.test.ts` + `run-now-action.test.ts` + `save-routine-action.test.ts` + `save-to-repo-action.test.ts` + `save-to-repo.test.ts` + `set-enabled-action.test.ts`. Stash-pop-verify confirmed: `pnpm test` with Plan 06-01 changes stashed = 50 failed / 308 passed (358 total); with changes applied = 50 failed / 313 passed (363 total); same 50 failures in both runs — Plan 06-01 causes zero regressions. All 50 logged as out-of-scope in `.planning/phases/06-polish/deferred-items.md` with per-file failure counts + parallel-session commit refs for the user to triage before Phase 6 exit gate. DOCS-02 requirement flipped from Pending → Complete (28/32 v1 requirements Complete). Plan executed exactly as written outside the documented parallel-session environmental complication — zero Rule 1 bugs, zero Rule 2 missing-critical auto-fixes, zero Rule 4 architectural decisions, zero auth gates. Downstream unlocked: Plan 06-03 (AUTHORING.md §2 can deep-link to concrete template paths). Note: ROADMAP previously listed Phase 6 as "6 plans"; actual on-disk count is 7 (06-01 through 06-07); corrected in this update.)

*Previous update: 2026-04-21 after Phase 5 Plan 08 exit gate (Phase 5 SEALED — 8/8 plans Complete; QUEU-01 + QUEU-02 + QUEU-03 + QUEU-04 + SAFE-01 all flipped to Complete; milestone progress 4/6 → 5/6 phases sealed. Single docs-only `docs(05)` commit captures 05-VALIDATION.md flip to approved 2026-04-21 with all 48 rows ✅ green + 05-SUMMARY.md phase rollup + 05-08-SUMMARY.md plan closeout + REQUIREMENTS.md 5 rows flipped + ROADMAP.md Phase 5 sealed + STATE.md milestone bar 4/6 → 5/6 + docs/activity_log.md entry. Automated 4-step exit gate green: (1) `cd dashboard && pnpm run typecheck` exit 0; (2) `pnpm test` 358/358 across 40 files in 10.62s; (3) `bash hooks/tests/supervisor-tests.sh` 36/36 across 30 scenarios (`all supervisor tests passed`); (4) `bash hooks/tests/run-tests.sh` 29/29. Dynamic PHASE5_BASE resolved via sentinel file `dashboard/tests/supervisor-runs.test.ts` first-add commit parent = `3c81b4f^` = `37ab5d9` (the 05-02 seal commit). Frozen-surface diff 0 lines across 31 enumerated v0.1 + Phase 2/3/4 paths — **critical correction from plan-check note #2**: stale hook filename references `sleepwalker-defer-run.sh` and `sleepwalker-budget-spent.sh` in the plan's `<interfaces>` block were replaced with actual repo filenames `sleepwalker-defer-irreversible.sh` and `sleepwalker-budget-cap.sh` before running the diff; without this fix the gate would have passed vacuously (git diff on nonexistent file returns 0 lines regardless). 3 documented QUEU-04 exceptions on v0.1 surface audited additive-only: `bin/sleepwalker-run-cli` +22/-5 (FD-form `( flock -w 5 -x 200 || true ; printf ... ) 200>"$LOCK_FILE"` wrap on audit_emit; both printf branches preserved byte-identical — supervisor JSON shape grep `"ts":"%s","fleet":"%s","runtime":"%s","event":"%s"` count=2), `hooks/sleepwalker-audit-log.sh` +13/-1 (FD-form flock wrap on jq -nc append; hook JSON shape grep `jq -nc` count=1), `install.sh` +7/-0 (flock preflight immediately after v0.1 jq preflight; head -2 shows `#!/bin/bash` shebang preserved, `set -euo pipefail` count=1, v0.1 preserved steps `Copying hooks to|Wiring hooks into|Initialize state directory` count=3). Shared-sidecar lock path byte-identical across writers: `diff <(grep LOCK_FILE= bin/sleepwalker-run-cli) <(grep LOCK_FILE= hooks/sleepwalker-audit-log.sh)` empty — one kernel mutex, two writers. SAFE-01 negative invariant `grep -rn 'budget.*tokens|tokens.*budget' dashboard/app/` empty. Plan 05-08 test harness additive scenarios (supervisor-tests 28 → 30 = +Scenario 8 4-way concurrency + Scenario 9 graceful fallthrough; run-tests 26 → 29 = +concurrent-audit scenarios) treated as additive hardening per CLAUDE.md §test files are not "public surface". v0.1 CONCERNS.md §concurrent-JSONL race officially CLOSED. Next: `/gsd-plan-phase 6` to plan Polish (DOCS-01, DOCS-02, DOCS-03, COMP-01, COMP-02).*

*Previous update: 2026-04-20 after Phase 5 Plan 07 execution (Wave 3 — SAFE-01 UI sweep + QUEU-02 consumer wiring sealed. 7/8 Phase 5 plans complete. Two atomic commits: `8eebb80` `fix(05-07): replace 'tokens' with 'chars (approximate)' in budget copy` (2 files / +4/-1 — `dashboard/app/routines/routines-client.tsx:62` `tokens` → `chars (approximate)` per PROJECT.md anti-feature ban + `dashboard/app/editor/editor-client.tsx` gains `Approximate character cap. Tokens vary by ±40% depending on output format.` helper `<p>` below budget input); `373b342` `feat(05-07): queue-client SourcePill + supervisor-run ActionDetail + jsdom tests` (2 files / +216/-12; 1 new — `dashboard/app/queue-client.tsx` gains inline exported SourceIcon + SourcePill 4-way dispatch (cloud / codex / gemini / local) replacing binary `isCloud` ternary; ActionDetail supervisor-run branch before generic kind-fallback renders labeled Event/Preview/Reason/Exit code + `Stopped at {chars} chars (budget: {limit}, approximate)` for budget_exceeded; footer 3rd arm for `isSupervisorRun` shows `Terminal state — no action required. View full audit at /audit?fleet=<fleet>` — no Approve/Reject; RecentList gains `statusPillClass()` helper widening pill mapping for Plan 05-01's QueueStatus complete/failed + switches to `<SourcePill />` for visual parity; new `dashboard/tests/queue-client.test.tsx` 6 jsdom it() blocks — SourcePill 4 variants + SAFE-01 'approximate' budget_exceeded render + supervisor-run labeled-fields render). Dashboard suite 352 → 358 green across 39 → 40 files (+6 jsdom blocks). Typecheck + build exit 0. SAFE-01 negative invariant final: `grep -rn 'budget.*tokens|tokens.*budget' dashboard/app/` empty. QUEU-02 consumer surface sealed (05-02 CSS producer + 05-07 JSX consumer); SAFE-01 UI copy invariant enforced at 4 layers (shell grep, jsdom negative assertion, positive `toContain('approximate')`, action-step regex). 1 Rule 2 auto-fix (RecentList widening for QueueStatus complete/failed — plan-check soft-spot #4 flagged as executor-owned); 1 Rule 1 auto-fix (single-line JSX for budget_exceeded regex match); zero architectural deviations; zero Rule-4 checkpoints. Only Plan 05-08 (phase exit gate + VALIDATION flip + frozen-surface diff) remains. Prior: 2026-04-21 after Phase 5 Plan 04 execution (Wave 1 flock-wrap supervisor audit_emit sealed — QUEU-04 supervisor side). 4/8 Phase 5 plans complete. Single atomic commit `c139354` `feat(05-04): flock-wrap supervisor audit_emit + concurrency tests` modifies `bin/sleepwalker-run-cli` audit_emit function (FD-form flock `-w 5 -x 200` on sidecar `${HOME}/.sleepwalker/audit.jsonl.lock`, `|| true` graceful fallthrough on timeout preserving supervisor critical path, both printf JSON shape preserved byte-identical to Phase 2) + `hooks/tests/supervisor-tests.sh` +105 lines (Scenario 8: 4-way codex+gemini parallel supervisors → exactly 8 lines, jq -e . per-line round-trip, all 4 fleets observed; Scenario 9: 6s flock holder forces 5s waiter to time out → `|| true` fallthrough preserves both events + supervisor exit 0). supervisor-tests 28 → 30 scenarios (36/36 assertions pass); dashboard suite stays 352/352 green across 39 files — no TS touched; typecheck exit 0. T-05-01 concurrent-write tampering mitigated (Scenario 8 live proof on `/opt/homebrew/bin/flock` 0.4.0); T-05-03 timeout DoS mitigated (Scenario 9 live proof); T-05-02 + T-05-04 accepted per RESEARCH §1.5; T-05-05 deferred to Plan 05-06 install preflight. Cross-plan sidecar lock path `audit.jsonl.lock` now anchored in supervisor; Plan 05-05 will seal the hook-writer half on the same byte-identical path. Plan executed exactly as written — zero Rule 1/2/3 auto-fixes, zero architectural deviations, zero auth gates, zero Rule-4 checkpoints. Pre-existing untracked files (CLAUDE.md + 2 screenshot PNGs) preserved untouched via explicit per-path `git add bin/sleepwalker-run-cli hooks/tests/supervisor-tests.sh`. Next actions: Plan 05-05 (hook writer on shared sidecar lock — parallel-safe with 05-06) OR Plan 05-06 (SAFE-01 char-budget + install.sh preflight `command -v flock` check — closes T-05-05).*

*Previous update: 2026-04-21 after Phase 5 Plan 03 execution (Wave 1 readSupervisorRuns + 3-source aggregator merge sealed. 3/8 Phase 5 plans complete. Two atomic commits `3c81b4f` `feat(05-03): add readSupervisorRuns + 3-source aggregator merge` (`dashboard/lib/queue-aggregator.ts` 49→208 lines adding SupervisorAuditEntry + TERMINAL_EVENTS/SUPERVISOR_RUNTIMES allowlists + supervisorRunToQueueEntry mapper + readSupervisorRuns reader with 24h cutoff + 3-source aggregator merge + supervisorCount additive field; `dashboard/tests/supervisor-runs.test.ts` new 204 lines 12 it() blocks covering RESEARCH §6.1 matrix) + `a3e85e5` `test(05-03): assert aggregateQueue merges all 4 sources` (`dashboard/tests/queue-aggregator.test.ts` +81/0 4-source merge integration block). Dashboard suite 339 → 352 green across 38 → 39 files (+13). Typecheck exit 0. QUEU-03 byte-identical supervisor invariant verified (`git diff HEAD~2 HEAD -- bin/sleepwalker-run-cli` = 0 lines). PHASE5_BASE sentinel file established at `dashboard/tests/supervisor-runs.test.ts`. QUEU-03 reader-side surface code-complete; end-to-end QUEU-03 lands when Plan 05-07 wires `kind:"supervisor-run"` + `source:codex|gemini` into `queue-client.tsx` UI branch. Wave 0 COMPLETE + Wave 1 partial — 05-04 flock wrap remains in Wave 1; 05-05 + 05-06 SAFE-01 UI parallel-safe at Wave 2. Zero Rule 1/2/3 auto-fixes; one additive tsCompact regex refinement for millisecond timestamps documented in SUMMARY §Deviations; zero architectural deviations; zero auth gates.)*

*Previous update: 2026-04-20 after Phase 5 Plan 02 execution (Wave 0 COMPLETE — `.pill-codex` + `.pill-gemini` Tailwind utility classes added to `dashboard/app/globals.css:78-79` inside `@layer components`. 2/8 Phase 5 plans complete. Single atomic commit `548d432` `feat(05-02): add pill-codex + pill-gemini utility classes` — 1 file changed / +2/-0 insertions. `.pill-codex` resolves to `@apply pill bg-aurora-500/10 text-aurora-400 border border-aurora-500/30` (deeper blue than cloud's aurora-400 — effective contrast ~7.8:1 on ink-900 per RESEARCH §4.3, WCAG AA pass); `.pill-gemini` resolves to `@apply pill bg-dawn-400/10 text-dawn-400 border border-dawn-400/30` (warm highlight tone — effective contrast ~11.0:1 on ink-900, WCAG AA pass). Pure CSS — zero new palette tokens (aurora.500 + dawn.400 both pre-exist in tailwind.config.js), zero new dependencies, zero JSX changes, zero new test files (Wave 3 Plan 05-07 jsdom matrix covers the class-attachment assertion per plan objective). `cd dashboard && pnpm run build` exits 0 with no new warnings (Tailwind JIT compiles the new `@apply` directives cleanly); route bundle sizes UNCHANGED across `/` / `/editor` / `/routines` because no consumer uses the classes yet. `pnpm run typecheck` exits 0. Dashboard suite stays 339/339 green across 38 files. VALIDATION rows 5/6/7 (05-02 Per-Task Verification Map) all pass. Acceptance-criteria greps validate `.pill-codex` count = 1, `.pill-gemini` count = 1, `bg-aurora-500/10` and `bg-dawn-400/10` present, palette file untouched, awk ordering guard returns ORDER_OK (77 < 78 < 79 < 80). Plan executed exactly as written — zero Rule 1/2/3 auto-fixes, zero architectural deviations, zero auth gates. QUEU-02 CSS producer surface is code-complete at the stylesheet boundary; end-to-end QUEU-02 consumer surface lands when Plan 05-07 (UI source branch in queue-client.tsx) maps `source === "codex"` / `source === "gemini"` → the respective pill classes. Wave 0 COMPLETE — Plan 05-03 (Wave 1 `readSupervisorRuns` supervisor audit reader, QUEU-03) is the next available plan, dep-cleared by 05-01's widened `QueueSource` / `QueueStatus` unions.)

*Previous update: 2026-04-20 after Phase 5 Plan 01 execution (Wave 0 foundation — QueueSource + QueueStatus union widen sealed. 1/8 Phase 5 plans complete. Single atomic commit `a545f0b` feat(05-01): widened `QueueSource` union to `"local" | "cloud" | "codex" | "gemini"` (QUEU-01) and `QueueStatus` union to add `"complete"` + `"failed"` terminal supervisor-run states. Strictly additive — RESEARCH §3.2 grep-audit prediction validated by `pnpm run typecheck` exiting 0 with zero consumer-side edits required. `dashboard/tests/queue.test.ts` gained 3 new `it()` blocks (plan target `>=2`) asserting codex + complete round-trip, gemini + failed round-trip, and gemini + complete combined smoke all survive JSON round-trip through `appendQueueEntry` + `readLocalQueue`. Dashboard suite 336 → 339 green across 38 files (queue.test.ts 7 → 10 blocks). QUEU-01 type surface is code-complete at the lib boundary; requirement remains Pending/Partial until Plan 05-03 (`readSupervisorRuns` supervisor audit reader) and Plan 05-07 (queue-client.tsx UI source branch) complete. Wave 0 partial — Plan 05-02 pill CSS is the sibling Wave 0 plan, parallel-safe with Plan 05-03 at the Wave 0/1 boundary.)*

*Previous update: 2026-04-20 after Phase 4 Plan 09 execution (Wave 4 — phase exit gate sealed. 9/9 Phase 4 plans complete. Commits `1f1feb6` + `71f920d` wire RoutineActionBar + StatusPill + drift-aware listRoutinesAsync into /routines; mount HealthBadgeRow on landing PageHeader meta; add routines-page.test.ts integration test with 4 it() blocks covering VALIDATION row 11 "status per bundle" for drift / deployed / draft flows. Phase 4 automated exit gate green: typecheck exit 0, dashboard suite 332 → 336 green (+4 blocks), supervisor harness 28/0 passing (no Phase 2 regression), frozen-surface diff 0 lines across 24 enumerated v0.1 + Phase 2/3 paths vs PHASE4_BASE `8707433^`. All 7 Phase 4 requirements (DEPL-01 + DEPL-02 + DEPL-03 + DEPL-04 + DEPL-05 + REPO-01 + HLTH-01) flipped from Pending/Partial to Complete. Milestone progress: 4/6 phases sealed. Next: `/gsd-plan-phase 5` for Phase 5 Queue.)*

*Previous update: 2026-04-20 after Phase 4 Plan 08 execution (Wave 3 — save-to-repo UI triad landed; commits `b09ab93` feat DiffStatPanel (80 lines) + ConfirmDialog (114 lines) presentational components — DiffStatPanel renders per-file grid-cols-[1fr_auto] rows with text-signal-green +{added} / text-signal-red −{removed} and UI-SPEC-locked empty-state copy `No staged changes — this bundle is already in sync with HEAD.` + non-empty heading `{n} file{s?} changed — {added} additions, {removed} deletions`; ConfirmDialog shared role=dialog aria-modal with focus-on-cancel + Esc close + backdrop click close + AnimatePresence fade+scale + btn-ghost/btn-danger CTAs, placed at `app/_components/` for 04-09 DisableToggle cross-segment import; `feadcd6` feat SaveToRepoModal (555 lines across SaveToRepoModal + ReviewStage + ConfirmStage sub-components) two-stage Review→Confirm with owned flock lifecycle — previewSaveToRepoAction on mount acquires lockToken (invokedRef Strict Mode guard), Stage 1 branches preview===null/loading/ok/lock-busy/no-changes/git-error, Stage 2 commit-message textarea with INPUT_OPT_OUT 8-attr bag + Cmd/Ctrl+Enter submit + never-push subtitle `This writes a local commit. Push manually with \`git push\` when you're ready.` + post-commit green 6s toast `Committed {shortSha} — {message.split('\n')[0]}`; Discard via ConfirmDialog `Discard this save?` prompt; safe-unmount release when preview.ok && !committed; focus management across stages; Esc routing between stages; dashboard suite stays 332/332 green across 37 files — zero new test files by plan design (Plan 04-05 real-git matrix + Plan 04-02 lib matrix cover save-to-repo boundary end-to-end; jsdom mocking of save-to-repo.ts module-scoped flock registry infeasible without heavy scaffolding; VALIDATION.md §Manual-Only row 4 two-tab lock-busy resolves by inspection against grep-verifiable UI copy); typecheck exit 0; `pnpm build` `/routines` 2.14 kB / 143 kB and `/editor` 13.2 kB / 154 kB UNCHANGED — modal + dialog + panel authored but not yet mounted; Plan 04-09 wires SaveToRepoModal trigger into routines-client + ConfirmDialog-backed DisableToggle; client-bundle safety check passes (grep `node:|from "fs"|from "os"|from "path"` returns 0 hits); REPO-01 UI surface code-complete at component level; Phase 4 now 8/9 plans done (89%); only Wave 4 04-09 exit gate remains. Pre-existing parallel-session uncommitted paths preserved untouched via explicit staging. Zero Rule 1/2/3 auto-fixes; zero architectural deviations; zero auth gates; two minor in-scope refinements documented in 04-08-SUMMARY.md §Deviations (ConfirmStage+ReviewStage factored as local sub-components for readability; committed boolean flag suppresses duplicate unmount release — behavior-neutral))*
