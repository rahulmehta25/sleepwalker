---
phase: 03-editor
plan: 09
type: execute
wave: 6
depends_on: [03-01, 03-02, 03-03, 03-04, 03-05, 03-06, 03-07, 03-08]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/03-editor/03-VALIDATION.md
autonomous: true
requirements: [EDIT-01, EDIT-02, EDIT-03, EDIT-04, EDIT-05]
tags: [phase-exit, frozen-surface, roadmap]
must_haves:
  truths:
    - "Full dashboard suite green: pnpm typecheck + pnpm test both clean"
    - "v0.1 frozen surface diff against PHASE2_BASE = 0 lines across all 14 enumerated paths"
    - "03-VALIDATION.md: every Task ID cell filled, nyquist_compliant flipped to true, status: approved"
    - "ROADMAP Phase 3 row flipped to Complete with plan count"
    - "STATE.md updated: Phase 3 shows 9/9 plans complete (milestone 2/6 phases → 3/6 after seal)"
  artifacts:
    - path: .planning/ROADMAP.md
      provides: "Phase 3 marked complete; plan list shows all 8 plans with commit SHAs"
      contains: "Phase 3 Editor"
    - path: .planning/STATE.md
      provides: "Milestone progress reflects Phase 3 sealed; Phase 4 Deploy is now critical path"
      contains: "Phase 3"
    - path: .planning/phases/03-editor/03-VALIDATION.md
      provides: "Every row has a 3-NN-MM Task ID; status approved"
      contains: "nyquist_compliant: true"
  key_links:
    - from: git diff PHASE2_BASE HEAD
      to: 14 v0.1 paths
      via: "0-line diff"
      pattern: "^0\\s+0"
---

<objective>
Phase 3 exit gate. Verify the full dashboard suite is green, verify the v0.1 frozen surface has not been touched (0-line diff against the Phase 2 sealed base across the 14 enumerated v0.1 paths), fill in every Task ID cell in 03-VALIDATION.md, flip the ROADMAP Phase 3 row to Complete, and update STATE.md for the transition to Phase 4.

Purpose: The rest of Phase 3 is a sequence of feature commits. The exit gate is the single authoritative check that the phase is done correctly. Without this plan, Phase 4 planner has no deterministic "Phase 3 ready" signal.

Output: 3 files modified; no new code; commit is documentation-only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-editor/03-VALIDATION.md
</context>

<interfaces>
<!-- 14 v0.1 frozen paths (from CLAUDE.md Architecture section + Phase 1 01-04 gate).
     The exit gate runs `git diff --stat PHASE2_BASE HEAD -- <paths>` and asserts output begins with "0" or is empty.

PHASE2_BASE resolution:
  - PHASE2_BASE = commit sha of Phase 2 Plan 02-10 exit (the commit that flipped ROADMAP Phase 2 → Complete).
  - Use `git log --oneline -- .planning/ROADMAP.md` to find that commit or read STATE.md after Phase 2 sealed.
  - If Phase 2 is not yet sealed at the time this plan runs, the gate uses the tag/sha recorded in STATE.md; if no tag exists use the most recent commit BEFORE Phase 3 Plan 01 started (e.g. commit prior to `.planning/phases/03-editor/03-01-PLAN.md` first execution).

14 frozen paths (exact):
  install.sh
  hooks/sleepwalker-pretooluse-defer.sh
  hooks/sleepwalker-posttooluse-budget.sh
  hooks/sleepwalker-posttooluse-audit.sh
  hooks/_detect_fleet.sh
  routines-local/sleepwalker-inbox-triage/SKILL.md
  routines-local/sleepwalker-finance-reconcile/SKILL.md
  routines-local/sleepwalker-code-review-nightly/SKILL.md
  routines-local/sleepwalker-github-issue-triage/SKILL.md
  routines-local/sleepwalker-weekly-digest/SKILL.md
  routines-local/sleepwalker-morning-brief/SKILL.md
  routines-cloud/  (all 8 v0.1 cloud routines as a directory — may be compressed to the parent)
  dashboard/lib/queue.ts   (QueueEntry fields — if touched this is a regression)
  ~/.claude/settings.json wiring — NOT in repo, but verify hooks/*.sh absolute paths unchanged
-->
</interfaces>

<validation>
This plan covers phase-exit rows 24 and 25 of `.planning/phases/03-editor/03-VALIDATION.md`.

After this plan, every row's Task ID column is filled and `nyquist_compliant: true` + `status: approved` are set in the frontmatter.
</validation>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Resolve PHASE2_BASE and run frozen-surface diff</name><name>Task 1: Resolve PHASE2_BASE and run frozen-surface diff</name>
  <read_first>
    - .planning/STATE.md (most recent decisions block — look for "Phase 2 sealed" entry)
    - .planning/ROADMAP.md (Phase 2 status)
    - CLAUDE.md §Architecture §Frozen backward-compat surface (14 paths list)
  </read_first>
  <files>n/a (verification only)</files>
  <action>
    1. Determine PHASE2_BASE:
       ```bash
       # Find the commit that marked Phase 2 complete in ROADMAP.md
       PHASE2_BASE=$(git log --format='%H' -- .planning/ROADMAP.md | while read sha; do
         if git show "$sha:.planning/ROADMAP.md" 2>/dev/null | grep -q "2\. Adapters.*Complete"; then
           echo "$sha"; break
         fi
       done)
       echo "PHASE2_BASE=$PHASE2_BASE"
       ```
       If no such commit found (Phase 2 not yet sealed at time of Phase 3 execution), fall back to the parent of Phase 3's first commit:
       ```bash
       PHASE3_FIRST=$(git log --format='%H' --reverse --diff-filter=A -- .planning/phases/03-editor/03-01-PLAN.md | head -1)
       PHASE2_BASE=$(git rev-parse "${PHASE3_FIRST}^")
       ```

    2. Run the frozen-surface diff:
       ```bash
       git diff --stat "$PHASE2_BASE" HEAD -- \
         install.sh \
         hooks/sleepwalker-pretooluse-defer.sh \
         hooks/sleepwalker-posttooluse-budget.sh \
         hooks/sleepwalker-posttooluse-audit.sh \
         hooks/_detect_fleet.sh \
         routines-local/ \
         routines-cloud/ \
         dashboard/lib/queue.ts
       ```
       Expected: empty output OR a summary line showing `0 files changed, 0 insertions(+), 0 deletions(-)`.

       If ANY diff appears: STOP. This is a regression. Do not proceed with exit-gate flip. Document the diff and return a FAILED exit-gate status.

    3. Record the command + output in a scratchpad for Task 3's SUMMARY.

    No files modified in this task — it's a verification gate only.
  </action>
  <verify>
    <automated>test -n "$(git log --format='%H' -- .planning/ROADMAP.md | head -1)" && echo OK</automated>
    <automated>git diff --stat HEAD -- install.sh hooks/ routines-local/ routines-cloud/ dashboard/lib/queue.ts 2>&1 | grep -cE "file[s]? changed|^$" || echo 0</automated>
  </verify>
  <done>
    PHASE2_BASE commit SHA resolved + printed. `git diff --stat PHASE2_BASE HEAD -- <14 paths>` returns 0-line diff or empty output. If any regression surfaced, plan execution HALTED with a clear error before proceeding to Task 2.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run full dashboard suite + typecheck</name><name>Task 2: Run full dashboard suite + typecheck</name>
  <read_first>
    - dashboard/package.json (test script)
  </read_first>
  <files>n/a (verification only)</files>
  <action>
    1. `cd dashboard && pnpm typecheck` → MUST exit 0.
    2. `cd dashboard && pnpm test` → MUST report all tests passing. Expected count: prior 72 baseline + approximately 12 (bundle-schema) + 14 (secret-scan) + 15 (bundles) + 7 (atomic-write) + 12 (save-routine-action) + 6 (runtime-radio-grid) + 4 (cron-preview) + 6 (draft-recovery-banner) + 10 (editor-client) = ~158 tests. Accept any number ≥ 155 as within drift budget.
    3. Record exact `Tests  N passed (N)` line for SUMMARY.

    If typecheck or test fails, HALT before proceeding to Task 3.
  </action>
  <verify>
    <automated>cd dashboard && pnpm typecheck 2>&1 | tail -5 | grep -v "error" | head -1</automated>
    <automated>cd dashboard && pnpm test 2>&1 | grep -E "Tests\s+\d+\s+passed"</automated>
  </verify>
  <done>
    pnpm typecheck exits 0. pnpm test reports ≥ 155 passing. No failing or skipped tests.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Fill 03-VALIDATION.md Task IDs + flip ROADMAP + update STATE.md</name>
  <read_first>
    - .planning/phases/03-editor/03-VALIDATION.md (current state — all 25 rows Task ID = TBD)
    - .planning/ROADMAP.md (Phase 3 row + Phases list)
    - .planning/STATE.md (Current Position + Milestone progress)
    - Plan SUMMARYs for cross-reference: 03-01 through 03-07 (each records which rows it satisfied)
  </read_first>
  <files>.planning/phases/03-editor/03-VALIDATION.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>
    1. Update `.planning/phases/03-editor/03-VALIDATION.md`:
       - Frontmatter: `status: approved`, `nyquist_compliant: true`, add `approved: 2026-04-NN by planner` comment line
       - Fill every `Task ID` cell per this plan-to-row map:
         | Row | Req | Task ID |
         |-----|-----|---------|
         | 1   | EDIT-01 | 3-08-01 |
         | 2   | EDIT-01 | 3-06-02 |
         | 3   | EDIT-01 | 3-06-03 |
         | 4   | EDIT-02 | 3-01-02 |
         | 5   | EDIT-02 | 3-02-02 |
         | 6   | EDIT-02 | 3-04-01 |
         | 7   | EDIT-02 | 3-05-02 |
         | 8   | EDIT-02 | 3-05-02 |
         | 9   | EDIT-02 | 3-05-02 |
         | 10  | EDIT-03 | 3-08-01 |
         | 11  | EDIT-03 | 3-08-01 |
         | 12  | EDIT-03 | 3-08-01 |
         | 13  | EDIT-03 | 3-07-02 |
         | 14  | EDIT-04 | 3-01-02 |
         | 15  | EDIT-04 | 3-01-02 |
         | 16  | EDIT-04 | 3-01-02 |
         | 17  | EDIT-04 | 3-01-02 |
         | 18  | EDIT-04 | 3-05-02 |
         | 19  | EDIT-04 | 3-05-02 |
         | 20  | EDIT-04 | 3-03-01 |
         | 21  | EDIT-04 | 3-05-02 |
         | 22  | EDIT-05 | 3-08-01 |
         | 23  | EDIT-05 | 3-08-01 |
         | 24  | Phase exit | 3-09-02 |
         | 25  | Phase exit | 3-09-01 |
       - Set Plan column: e.g. `03-01`, `03-02`, …, `03-09` according to the Task ID.
       - Tick the "Validation Sign-Off" checklist box `[x]` for each criterion now satisfied.

    2. Update `.planning/ROADMAP.md`:
       - In the Phases list, change `- [ ] **Phase 3: Editor**` to `- [x] **Phase 3: Editor**`.
       - In the Phase 3 Details block: update `**Plans**: TBD` to `**Plans**: 9 plans` and fill the Plans sub-list with each of the 9 03-NN-PLAN.md files + a one-line description + `completed YYYY-MM-DD (commit <sha>)` for each.
       - In the Progress table at the bottom, change Phase 3 row: `0/TBD | Not started | -` to `9/9 | Complete | 2026-04-NN`.

    3. Update `.planning/STATE.md`:
       - Advance `**Phase:** 2 — Adapters (in progress)` → `**Phase:** 3 — Editor (complete); next: Phase 4 Deploy` (or the equivalent wording matching file style).
       - Append an entry to `### Decisions`:
         `- **2026-04-NN** — Phase 3 Editor sealed. All 5 EDIT-0N requirements closed. Dashboard suite 72 → ~158 green. v0.1 frozen surface diff = 0 lines against PHASE2_BASE <sha>. `/editor` route live with atomic-write, secret-scan, autosave, collision check, autofill opt-out. Next critical path: Phase 4 Deploy (wire adapters through UI).`
       - Update the `**Milestone progress:**` bar to `[##----] 3/6 phases complete` (assuming Phase 2 completes before Phase 3; adjust if Phase 2 not sealed at this moment).
       - Update Phase 3 progress bar to `[#########] 9/9 plans complete`.
       - Append a row to the Plans table: `| 03-01 | … | 2 | 5 | <sha> |` etc. for each of the 9 plans (use commit SHAs recorded in each plan's SUMMARY).
       - Update `**Performance Metrics**` table: increment Plans complete (6 → 14), Plans authored (14 → 22 if this plan batches ran in pure Phase 3 sequence — adjust based on actual), Requirements complete (3 → 8, adding EDIT-01..05).
       - Mark the open todo `[ ] Plan Phase 3 (Editor)…` as `[x]`. Add a new `[ ] Plan Phase 4 (Deploy) …` todo.

    4. Single atomic commit:
       ```
       git add .planning/phases/03-editor/03-VALIDATION.md .planning/ROADMAP.md .planning/STATE.md
       git commit -m "docs(03-09): phase 3 editor exit gate passed"
       ```

    5. Update docs/activity_log.md per CLAUDE.md rule with the exit gate action.
  </action>
  <verify>
    <automated>grep -c "nyquist_compliant: true" .planning/phases/03-editor/03-VALIDATION.md</automated>
    <automated>grep -c "status: approved" .planning/phases/03-editor/03-VALIDATION.md</automated>
    <automated>grep -cE "^\| \d+\s+\| EDIT-0[1-5]\s+\| 3-0[0-9]-0[0-9] " .planning/phases/03-editor/03-VALIDATION.md</automated>
    <automated>grep -cE "- \[x\] \*\*Phase 3: Editor\*\*" .planning/ROADMAP.md</automated>
    <automated>grep -cE "9/9\s*\|\s*Complete" .planning/ROADMAP.md</automated>
    <automated>grep -c "Phase 3 Editor sealed" .planning/STATE.md</automated>
  </verify>
  <done>
    03-VALIDATION.md: all 25 Task IDs filled, nyquist_compliant=true, status=approved, sign-off checkboxes ticked.
    ROADMAP.md: Phase 3 row ticked; Plans sub-list populated with 8 entries + commit SHAs; Progress table shows 8/8 Complete.
    STATE.md: phase advanced, milestone progress bar updated, decisions block appended, plans table has 8 new rows, performance metrics updated, next-phase todo added.
    All three files committed in a single documentation commit. Activity log updated.
  </done>
</task>

</tasks>

<verification>
- Full test suite still green after commits land (no code changes in this plan, but a re-run confirms).
- Phase 3 is discoverable as complete via `grep -E "^- \[x\] \*\*Phase 3" .planning/ROADMAP.md`.
- 03-VALIDATION.md approval status parse-able from frontmatter.
- v0.1 frozen-surface diff = 0 lines (verified in Task 1 before any doc flip).
</verification>

<success_criteria>
- [ ] PHASE2_BASE resolvable from git history or STATE.md
- [ ] `git diff --stat PHASE2_BASE HEAD -- <14 paths>` returns 0-line diff
- [ ] pnpm typecheck green; pnpm test green with ≥ 155 tests
- [ ] 03-VALIDATION.md: 25/25 Task IDs filled, status approved, nyquist_compliant true
- [ ] ROADMAP Phase 3 row flipped with accurate plan count (9) + commit SHAs
- [ ] STATE.md milestone + phase + plans table updated; Phase 3 Editor sealed decision recorded
- [ ] Single doc commit lands the flip
- [ ] Activity log updated
</success_criteria>

<output>
After completion, write `.planning/phases/03-editor/03-09-SUMMARY.md`:
- PHASE2_BASE sha used for diff
- Exact pnpm test count (expected ~158)
- Commit SHA of the doc-only exit-gate commit
- List of Phase 4 open questions inherited (if any — e.g. Deploy state-machine rollback guarantees)
- Pointer: Phase 4 planner can start with `/gsd-plan-phase 4` against the sealed Phase 3 state.
</output>
