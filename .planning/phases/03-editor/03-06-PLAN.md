---
phase: 03-editor
plan: 06
type: execute
wave: 3
depends_on: [03-05, phase-2-plan-02-09]
files_modified:
  - dashboard/app/editor/page.tsx
  - dashboard/app/editor/_components/runtime-radio-grid.tsx
  - dashboard/app/editor/_components/cron-preview.tsx
  - dashboard/app/editor/_components/secret-scan-panel.tsx
  - dashboard/app/editor/_components/draft-recovery-banner.tsx
  - dashboard/app/editor/_components/preview-panel.tsx
  - dashboard/tests/runtime-radio-grid.test.tsx
  - dashboard/tests/cron-preview.test.tsx
  - dashboard/tests/draft-recovery-banner.test.tsx
autonomous: true
requirements: [EDIT-01]
tags: [editor, ui, presentational, jsdom]
must_haves:
  truths:
    - "/editor route renders the PageHeader with locked UI-SPEC copy + EditorClient prop'd with healthStatuses from healthCheckAll()"
    - "RuntimeRadioGrid renders 4 cards; unavailable card has opacity-40 + cursor-not-allowed + pill-amber with reason tooltip; selected card has ring-1 ring-dawn-400"
    - "CronPreview shows `Runs {cronstrue}` on valid cron, UI-SPEC red message on invalid cron"
    - "SecretScanPanel renders red-bordered panel with exact UI-SPEC heading + body template + inline code fix example"
    - "DraftRecoveryBanner reads localStorage on mount and shows aurora banner if draft present; disappears after Restore or Start Fresh"
    - "PreviewPanel wraps toBundleDir/toPlistPath/toMarkerTag calls in try/catch so in-flight typing (invalid slug) doesn't crash"
  artifacts:
    - path: dashboard/app/editor/page.tsx
      provides: "Server Component shell; calls healthCheckAll()"
      min_lines: 20
    - path: dashboard/app/editor/_components/runtime-radio-grid.tsx
      provides: "2×2 radio-card grid with health pills"
      min_lines: 80
    - path: dashboard/app/editor/_components/cron-preview.tsx
      provides: "cronstrue pill / error inline"
      min_lines: 30
    - path: dashboard/app/editor/_components/secret-scan-panel.tsx
      provides: "Red-bordered panel with fix example"
      min_lines: 45
    - path: dashboard/app/editor/_components/draft-recovery-banner.tsx
      provides: "localStorage-backed aurora banner"
      min_lines: 60
    - path: dashboard/app/editor/_components/preview-panel.tsx
      provides: "Sticky right-column path preview"
      min_lines: 50
  key_links:
    - from: dashboard/app/editor/page.tsx
      to: dashboard/lib/runtime-adapters/index
      via: "healthCheckAll() call"
      pattern: "healthCheckAll"
    - from: dashboard/app/editor/_components/preview-panel.tsx
      to: dashboard/lib/runtime-adapters/slug
      via: "toBundleDir / toPlistPath / toMarkerTag builders (try/catch wrapped)"
      pattern: "toBundleDir|toPlistPath|toMarkerTag"
    - from: dashboard/app/editor/_components/runtime-radio-grid.tsx
      to: HealthStatus.warning
      via: "render pill-amber when warning set"
      pattern: "warning"
---

<objective>
Land the `/editor` Server Component shell and five presentational subcomponents. Each component is fully testable in isolation (jsdom tests for three of them). No form state machine, no autosave, no Server Action wiring in this plan — that's Plan 03-07. This plan delivers the visual contract from 03-UI-SPEC.md and the component inventory in §Component Inventory.

Purpose: Split the UI into two plans because a single plan covering shell + sub-components + state machine + autosave + integration tests would consume > 50% context. Sub-components ship first because Plan 03-07's EditorClient imports them — Plan 03-07 gets smaller and higher-quality by having these presentational pieces already green.

Output: 6 new components, 3 new .test.tsx files (first jsdom tests in the repo); test suite grows by ~16 it() blocks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/03-editor/03-UI-SPEC.md
@.planning/phases/03-editor/03-RESEARCH.md
@.planning/phases/03-editor/03-PATTERNS.md
@.planning/phases/03-editor/03-VALIDATION.md
@.planning/phases/02-adapters/02-CONTEXT.md
@dashboard/app/settings/page.tsx
@dashboard/app/settings/settings-client.tsx
@dashboard/app/_components/page-header.tsx
@dashboard/app/page.tsx
@dashboard/app/globals.css
@dashboard/lib/runtime-adapters/index.ts
@dashboard/lib/runtime-adapters/slug.ts
@dashboard/lib/runtime-adapters/types.ts
@dashboard/lib/bundles.ts
</context>

<interfaces>
<!-- Component contracts (locked by 03-UI-SPEC.md §Component Inventory + §Copywriting Contract): -->

// page.tsx (Server Component)
export const dynamic = "force-dynamic";
export default async function EditorPage() {
  // 1. const healthStatuses = await healthCheckAll();   // Record<Runtime, HealthStatus>
  // 2. const existingSlugs = listBundles().map(b => `${b.runtime}/${b.slug}`);
  // 3. <PageHeader eyebrow="AUTHORING" title="Author a routine" subtitle="Write a prompt, pick a runtime, pick a schedule. Save writes a validated bundle to disk." />
  // 4. <EditorClient healthStatuses={...} existingSlugs={...} />
}

// runtime-radio-grid.tsx
interface RuntimeCardInfo {
  runtime: Runtime;
  title: string;         // "Claude Routines" / "Claude Desktop" / "Codex Pro" / "Gemini CLI Pro"
  description: string;   // one-line per UI-SPEC lines 147-150
  icon: "Cloud" | "Workflow" | "ScrollText" | "ListChecks"; // lucide choice per runtime
}
interface Props {
  value: Runtime | "";
  onChange: (r: Runtime) => void;
  healthStatuses: Record<Runtime, HealthStatus>;
}

// cron-preview.tsx
interface Props { expression: string; }
// renders <span className="pill-aurora">Runs {parsed}</span> OR <span className="text-xs text-signal-red">Invalid cron — 5 fields required (minute hour day month weekday).</span>

// secret-scan-panel.tsx
interface Props { match: SecretMatch | null; onScrollToLine?: (line: number, column: number) => void; }
// renders nothing when match null; otherwise panel per UI-SPEC §Secret-scan error panel

// draft-recovery-banner.tsx
interface Draft { name: string; slug: string; runtime: string; updatedAt: string; }
interface Props { onRestore: (d: Draft) => void; onStartFresh: () => void; }
// useEffect reads localStorage.getItem("sleepwalker.draft.v1") on mount; renders banner only if draft exists

// preview-panel.tsx
interface Props { runtime: Runtime | ""; slug: string; schedule: string; healthStatus: HealthStatus | null; }
// wraps every builder call in try/catch (slug might be invalid during typing)
</interfaces>

<validation>
This plan's tasks cover these rows of `.planning/phases/03-editor/03-VALIDATION.md`:

| Row | Requirement | Task |
|-----|-------------|------|
| 2 | EDIT-01 | Task 2 — Unavailable runtimes dimmed with fix-tooltip |
| 3 | EDIT-01 | Task 3 — Cron input shows cronstrue preview |
| 13 | EDIT-03 | Task 5 — Draft-recovery banner shows when draft exists |

After this plan, `03-VALIDATION.md` Task ID column for rows 2, 3, 13 = `3-06-02`, `3-06-03`, `3-06-05`.
</validation>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author /editor page.tsx Server Component shell</name>
  <read_first>
    - dashboard/app/settings/page.tsx (exact analog — full file, copy import order verbatim)
    - dashboard/app/page.tsx (async Server Component awaiting multiple data sources)
    - dashboard/app/_components/page-header.tsx (component contract)
    - .planning/phases/03-editor/03-UI-SPEC.md lines 121-123 (exact header strings)
    - .planning/phases/03-editor/03-PATTERNS.md §page.tsx (lines 277-316)
    - dashboard/lib/runtime-adapters/index.ts (healthCheckAll() shape)
    - dashboard/lib/bundles.ts (listBundles shape)
  </read_first>
  <files>dashboard/app/editor/page.tsx</files>
  <action>
    Create `mkdir -p dashboard/app/editor/_components` first.

    Create `dashboard/app/editor/page.tsx`:
    ```tsx
    import { healthCheckAll } from "@/lib/runtime-adapters";
    import { listBundles } from "@/lib/bundles";
    import { PageHeader } from "../_components/page-header";
    import { EditorClient } from "./editor-client";

    export const dynamic = "force-dynamic";

    export default async function EditorPage() {
      const healthStatuses = await healthCheckAll();
      const existingSlugs = listBundles().map((b) => `${b.runtime}/${b.slug}`);

      return (
        <>
          <PageHeader
            eyebrow="AUTHORING"
            title="Author a routine"
            subtitle="Write a prompt, pick a runtime, pick a schedule. Save writes a validated bundle to disk."
          />
          <EditorClient healthStatuses={healthStatuses} existingSlugs={existingSlugs} />
        </>
      );
    }
    ```

    NOTE: This file imports `EditorClient` from `./editor-client` which DOES NOT exist yet (Plan 03-07 creates it). That's intentional — TypeScript will fail until 03-07 lands. Workaround for this plan ONLY: ship a minimal stub `dashboard/app/editor/editor-client.tsx` that just renders a placeholder, so typecheck passes. Plan 03-07 replaces it entirely.

    Create the stub `dashboard/app/editor/editor-client.tsx`:
    ```tsx
    "use client";
    import type { HealthStatus, Runtime } from "@/lib/runtime-adapters/types";

    interface Props {
      healthStatuses: Record<Runtime, HealthStatus>;
      existingSlugs: string[];
    }

    export function EditorClient(_props: Props): JSX.Element {
      // STUB — replaced wholesale in Plan 03-07 with the full form state machine.
      return <div data-testid="editor-client-stub" className="text-xs text-moon-600">editor-client pending plan 03-07</div>;
    }
    ```

    Run `pnpm typecheck` → clean.
    Commit: `feat(03-06): add /editor page.tsx Server Component shell + editor-client stub`.
  </action>
  <verify>
    <automated>grep -c "healthCheckAll" dashboard/app/editor/page.tsx</automated>
    <automated>grep -c "AUTHORING" dashboard/app/editor/page.tsx</automated>
    <automated>grep -c "Author a routine" dashboard/app/editor/page.tsx</automated>
    <automated>cd dashboard && pnpm typecheck 2>&1 | grep -c "error TS" || echo 0</automated>
  </verify>
  <done>
    `dashboard/app/editor/page.tsx` renders PageHeader with exact UI-SPEC strings and passes healthStatuses + existingSlugs into EditorClient. Stub editor-client exists to keep typecheck clean. TypeScript green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RuntimeRadioGrid component + jsdom test</name>
  <read_first>
    - .planning/phases/03-editor/03-UI-SPEC.md §Runtime radio-card grid (lines 245-251) + §Runtime picker copy (lines 144-152)
    - .planning/phases/03-editor/03-PATTERNS.md §runtime-radio-grid.tsx (lines 417-439)
    - dashboard/app/settings/settings-client.tsx lines 164-170 (panel + button pattern)
    - dashboard/app/globals.css @layer components (verify .panel, .panel-raised, .pill-green, .pill-amber, .pill-muted class names exist)
    - dashboard/lib/runtime-adapters/types.ts (HealthStatus shape — look for `available`, `reason`, `warning` fields)
  </read_first>
  <files>dashboard/app/editor/_components/runtime-radio-grid.tsx, dashboard/tests/runtime-radio-grid.test.tsx</files>
  <behavior>
    Tests first (TDD RED). Expected behaviors (React Testing Library + jsdom):
    - Renders exactly 4 radio cards with the exact titles from UI-SPEC: "Claude Routines", "Claude Desktop", "Codex Pro", "Gemini CLI Pro"
    - Available runtime → NO opacity-40 class; pill-green with "Ready" or similar available label
    - Unavailable runtime → has opacity-40 class; pill-amber OR pill-muted; reason text visible
    - Unavailable runtime radio input is `disabled`
    - Selected runtime has ring-1 ring-dawn-400 class
    - HealthStatus.warning set → pill-amber with warning text visible
    - Clicking available radio calls onChange with that runtime
    - Clicking disabled radio does NOT call onChange
    6 it() blocks minimum.
  </behavior>
  <action>
    RED — create `dashboard/tests/runtime-radio-grid.test.tsx`:
    ```tsx
    // @vitest-environment jsdom
    import { describe, it, expect, vi } from "vitest";
    import { render, screen, fireEvent } from "@testing-library/react";
    import { RuntimeRadioGrid } from "@/app/editor/_components/runtime-radio-grid";
    import type { HealthStatus, Runtime } from "@/lib/runtime-adapters/types";

    const allAvailable: Record<Runtime, HealthStatus> = {
      "claude-routines": { available: true },
      "claude-desktop":  { available: true },
      "codex":           { available: true },
      "gemini":          { available: true },
    };

    describe("RuntimeRadioGrid", () => {
      it("renders 4 cards with exact UI-SPEC titles", () => {
        render(<RuntimeRadioGrid value="" onChange={() => {}} healthStatuses={allAvailable} />);
        expect(screen.getByText("Claude Routines")).toBeTruthy();
        expect(screen.getByText("Claude Desktop")).toBeTruthy();
        expect(screen.getByText("Codex Pro")).toBeTruthy();
        expect(screen.getByText("Gemini CLI Pro")).toBeTruthy();
      });

      it("marks unavailable runtime with opacity-40 and disabled input", () => {
        const statuses = { ...allAvailable, codex: { available: false, reason: "codex not on PATH — npm i -g @openai/codex" } };
        const { container } = render(<RuntimeRadioGrid value="" onChange={() => {}} healthStatuses={statuses} />);
        const codexLabel = screen.getByText("Codex Pro").closest("label");
        expect(codexLabel?.className).toContain("opacity-40");
        const codexInput = container.querySelector('input[name="runtime"][value="codex"]') as HTMLInputElement;
        expect(codexInput?.disabled).toBe(true);
        expect(screen.getByText(/codex not on PATH/)).toBeTruthy();
      });

      it("adds ring-1 ring-dawn-400 to selected card", () => {
        render(<RuntimeRadioGrid value="codex" onChange={() => {}} healthStatuses={allAvailable} />);
        const codexLabel = screen.getByText("Codex Pro").closest("label");
        expect(codexLabel?.className).toMatch(/ring-1/);
        expect(codexLabel?.className).toMatch(/ring-dawn-400/);
      });

      it("renders pill-amber for HealthStatus.warning", () => {
        const statuses = { ...allAvailable, gemini: { available: true, warning: "Auth conflict: both subscription + env" } };
        render(<RuntimeRadioGrid value="" onChange={() => {}} healthStatuses={statuses} />);
        expect(screen.getByText(/Auth conflict/)).toBeTruthy();
      });

      it("calls onChange with runtime when available card clicked", () => {
        const onChange = vi.fn();
        const { container } = render(<RuntimeRadioGrid value="" onChange={onChange} healthStatuses={allAvailable} />);
        const codexInput = container.querySelector('input[name="runtime"][value="codex"]') as HTMLInputElement;
        fireEvent.click(codexInput);
        expect(onChange).toHaveBeenCalledWith("codex");
      });

      it("does not call onChange when disabled card clicked", () => {
        const onChange = vi.fn();
        const statuses = { ...allAvailable, codex: { available: false, reason: "n/a" } };
        const { container } = render(<RuntimeRadioGrid value="" onChange={onChange} healthStatuses={statuses} />);
        const codexInput = container.querySelector('input[name="runtime"][value="codex"]') as HTMLInputElement;
        fireEvent.click(codexInput);
        expect(onChange).not.toHaveBeenCalled();
      });
    });
    ```
    Run — MUST fail.

    GREEN — create `dashboard/app/editor/_components/runtime-radio-grid.tsx`:
    ```tsx
    "use client";
    import { Cloud, Workflow, ScrollText, ListChecks, AlertCircle } from "lucide-react";
    import clsx from "clsx";
    import type { HealthStatus, Runtime } from "@/lib/runtime-adapters/types";

    interface CardInfo {
      runtime: Runtime;
      title: string;
      description: string;
      Icon: typeof Cloud;
    }

    const CARDS: readonly CardInfo[] = [
      { runtime: "claude-routines", title: "Claude Routines", description: "Cloud — Anthropic infra, GitHub triggers.", Icon: Cloud },
      { runtime: "claude-desktop",  title: "Claude Desktop",  description: "Local — Desktop Scheduled Tasks.",       Icon: Workflow },
      { runtime: "codex",           title: "Codex Pro",       description: "Local — OpenAI codex CLI via launchd.",  Icon: ScrollText },
      { runtime: "gemini",          title: "Gemini CLI Pro",  description: "Local — Google gemini CLI via launchd.", Icon: ListChecks },
    ];

    interface Props {
      value: Runtime | "";
      onChange: (r: Runtime) => void;
      healthStatuses: Record<Runtime, HealthStatus>;
    }

    export function RuntimeRadioGrid({ value, onChange, healthStatuses }: Props): JSX.Element {
      return (
        <div className="grid grid-cols-2 gap-4">
          {CARDS.map((card) => {
            const status = healthStatuses[card.runtime];
            const available = status?.available ?? false;
            const selected = value === card.runtime;
            const hasWarning = available && Boolean(status?.warning);

            return (
              <label
                key={card.runtime}
                className={clsx(
                  "panel p-4 flex flex-col gap-2 cursor-pointer",
                  selected && "panel-raised ring-1 ring-dawn-400",
                  !available && "opacity-40 cursor-not-allowed",
                )}
              >
                <div className="flex items-center gap-2">
                  <card.Icon className="w-4 h-4" aria-hidden="true" />
                  <span className="text-sm font-medium">{card.title}</span>
                  <input
                    type="radio"
                    name="runtime"
                    value={card.runtime}
                    checked={selected}
                    disabled={!available}
                    onChange={() => onChange(card.runtime)}
                    className="sr-only"
                  />
                </div>
                <p className="text-xs text-moon-400">{card.description}</p>
                {available && !hasWarning && <span className="pill-green">Ready</span>}
                {available && hasWarning && (
                  <span className="pill-amber inline-flex items-center gap-1">
                    <AlertCircle className="w-3 h-3" aria-hidden="true" />
                    {status.warning}
                  </span>
                )}
                {!available && (
                  <span className="pill-amber text-xs">
                    {status?.reason ?? "Unavailable."}
                  </span>
                )}
              </label>
            );
          })}
        </div>
      );
    }
    ```

    Run `pnpm test runtime-radio-grid` → MUST pass 100%.
    Run `pnpm typecheck` → clean.
    Commit: `feat(03-06): add RuntimeRadioGrid + 6 jsdom tests`.
  </action>
  <verify>
    <automated>cd dashboard && pnpm test runtime-radio-grid.test.tsx 2>&1 | grep -E "Tests\s+\d+\s+passed"</automated>
    <automated>cd dashboard && pnpm typecheck 2>&1 | grep -c "error TS" || echo 0</automated>
    <automated>grep -c "Claude Routines\|Claude Desktop\|Codex Pro\|Gemini CLI Pro" dashboard/app/editor/_components/runtime-radio-grid.tsx</automated>
  </verify>
  <done>
    RuntimeRadioGrid ships with 4 cards using UI-SPEC titles/descriptions. Unavailable state dims + disables. Warning state shows amber pill. Selected card has dawn ring. 6 tests green. TypeScript clean. First jsdom tests in the repo working.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: CronPreview component + jsdom test</name>
  <read_first>
    - .planning/phases/03-editor/03-UI-SPEC.md §Cronstrue live preview (lines 317-322) + §Live-preview copy (lines 156-163)
    - .planning/phases/03-editor/03-PATTERNS.md §cron-preview.tsx (lines 443-460)
    - dashboard/app/globals.css (verify .pill-aurora exists)
  </read_first>
  <files>dashboard/app/editor/_components/cron-preview.tsx, dashboard/tests/cron-preview.test.tsx</files>
  <behavior>
    - valid cron "0 6 * * 1-5" → renders `Runs {cronstrue output}` in pill-aurora
    - invalid cron "" → renders UI-SPEC exact error: "Invalid cron — 5 fields required (minute hour day month weekday)." in signal-red
    - invalid cron "0 6 * *" (4 fields) → same error
    - "0 0 * * *" → renders some "Runs at 12:00 AM" variant (cronstrue output may vary — assert prefix "Runs" and pill-aurora class)
    4 it() blocks.
  </behavior>
  <action>
    RED — create `dashboard/tests/cron-preview.test.tsx`:
    ```tsx
    // @vitest-environment jsdom
    import { describe, it, expect } from "vitest";
    import { render, screen } from "@testing-library/react";
    import { CronPreview } from "@/app/editor/_components/cron-preview";

    describe("CronPreview", () => {
      it("renders pill-aurora with 'Runs …' on valid cron", () => {
        const { container } = render(<CronPreview expression="0 6 * * 1-5" />);
        const pill = container.querySelector(".pill-aurora");
        expect(pill).toBeTruthy();
        expect(pill?.textContent?.startsWith("Runs ")).toBe(true);
      });

      it("renders exact UI-SPEC error on empty expression", () => {
        const { container } = render(<CronPreview expression="" />);
        expect(screen.getByText("Invalid cron — 5 fields required (minute hour day month weekday).")).toBeTruthy();
        const err = container.querySelector(".text-signal-red");
        expect(err).toBeTruthy();
      });

      it("renders error on 4-field cron", () => {
        render(<CronPreview expression="0 6 * *" />);
        expect(screen.getByText("Invalid cron — 5 fields required (minute hour day month weekday).")).toBeTruthy();
      });

      it("renders Runs-prefix on midnight cron", () => {
        const { container } = render(<CronPreview expression="0 0 * * *" />);
        const pill = container.querySelector(".pill-aurora");
        expect(pill?.textContent?.startsWith("Runs ")).toBe(true);
      });
    });
    ```

    GREEN — create `dashboard/app/editor/_components/cron-preview.tsx`:
    ```tsx
    "use client";
    import cronstrue from "cronstrue";

    interface Props {
      expression: string;
    }

    const INVALID_MSG = "Invalid cron — 5 fields required (minute hour day month weekday).";

    export function CronPreview({ expression }: Props): JSX.Element {
      const trimmed = expression.trim();
      const fieldCount = trimmed.length === 0 ? 0 : trimmed.split(/\s+/).length;

      if (fieldCount !== 5) {
        return <span className="text-xs text-signal-red">{INVALID_MSG}</span>;
      }

      let parsed: string;
      try {
        parsed = cronstrue.toString(trimmed, { verbose: false, use24HourTimeFormat: true });
      } catch {
        return <span className="text-xs text-signal-red">{INVALID_MSG}</span>;
      }

      return <span className="pill-aurora">Runs {parsed}</span>;
    }
    ```

    Run `pnpm test cron-preview` → pass. typecheck clean.
    Commit: `feat(03-06): add CronPreview + 4 jsdom tests`.
  </action>
  <verify>
    <automated>cd dashboard && pnpm test cron-preview.test.tsx 2>&1 | grep -E "Tests\s+\d+\s+passed"</automated>
    <automated>cd dashboard && pnpm typecheck 2>&1 | grep -c "error TS" || echo 0</automated>
    <automated>grep -c "Invalid cron — 5 fields required" dashboard/app/editor/_components/cron-preview.tsx</automated>
  </verify>
  <done>
    CronPreview renders exactly UI-SPEC strings for both valid + invalid inputs. cronstrue integration proven. 4 tests green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: SecretScanPanel component (presentational, no test — covered by editor-client integration in Plan 03-07)</name>
  <read_first>
    - .planning/phases/03-editor/03-UI-SPEC.md §Secret-scan error panel (lines 262-274)
    - .planning/phases/03-editor/03-PATTERNS.md §secret-scan-panel.tsx (lines 466-477)
    - dashboard/lib/secret-scan.ts (SecretMatch shape)
    - dashboard/app/globals.css (verify .panel and signal-red utilities exist)
  </read_first>
  <files>dashboard/app/editor/_components/secret-scan-panel.tsx</files>
  <action>
    Create `dashboard/app/editor/_components/secret-scan-panel.tsx`:
    ```tsx
    "use client";
    import type { SecretMatch } from "@/lib/secret-scan";

    interface Props {
      match: SecretMatch | null;
      onScrollToLine?: (line: number, column: number) => void;
    }

    export function SecretScanPanel({ match, onScrollToLine }: Props): JSX.Element | null {
      if (!match) return null;

      return (
        <div className="panel border-signal-red/50 bg-signal-red/5 p-4 mt-2 flex flex-col gap-2">
          <h3 className="text-signal-red font-semibold text-sm">Secret detected — save blocked</h3>
          <p className="text-xs text-moon-200">
            {match.patternName} at line {match.line}, column {match.column}. Replace the matched substring with <code className="font-mono">${"{VAR}"}</code> and document the variable in AUTHORING.md.
          </p>
          <pre className="panel-raised text-xs font-mono p-2 whitespace-pre-wrap">
{`Before:  OPENAI_API_KEY=sk_live_abc123…
After:   OPENAI_API_KEY=\${OPENAI_API_KEY}`}
          </pre>
          {onScrollToLine && (
            <button
              type="button"
              className="text-xs text-dawn-400 underline-offset-2 hover:underline self-start"
              onClick={() => onScrollToLine(match.line, match.column)}
            >
              View matched region
            </button>
          )}
        </div>
      );
    }
    ```
    Run `pnpm typecheck` → clean.
    Commit: `feat(03-06): add SecretScanPanel presentational component`.

    NOTE: This component is covered by editor-client integration tests in Plan 03-07 (`editor-client.test.tsx -t "secret blocks save with visible panel"`). No dedicated .test.tsx authored here to keep Plan 03-06 under budget — the component is a pure pass-through of the SecretMatch + onScrollToLine contract and has no internal logic.
  </action>
  <verify>
    <automated>grep -c "Secret detected — save blocked" dashboard/app/editor/_components/secret-scan-panel.tsx</automated>
    <automated>grep -c "View matched region" dashboard/app/editor/_components/secret-scan-panel.tsx</automated>
    <automated>cd dashboard && pnpm typecheck 2>&1 | grep -c "error TS" || echo 0</automated>
  </verify>
  <done>
    SecretScanPanel renders exact UI-SPEC heading + body template + fix example + optional "View matched region" button. Renders null when match is null. TypeScript clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: DraftRecoveryBanner + jsdom test</name>
  <read_first>
    - .planning/phases/03-editor/03-UI-SPEC.md §Draft-recovery banner (lines 276-287)
    - .planning/phases/03-editor/03-PATTERNS.md §draft-recovery-banner.tsx (lines 480-499)
    - dashboard/app/_components/page-header.tsx (framer-motion wrap pattern)
    - dashboard/app/globals.css (verify .panel-raised + aurora-400 utilities)
  </read_first>
  <files>dashboard/app/editor/_components/draft-recovery-banner.tsx, dashboard/tests/draft-recovery-banner.test.tsx</files>
  <behavior>
    - No draft in localStorage → component renders null
    - Draft with version:1 in localStorage → banner renders with "You have an unsaved draft from" + metadata line
    - Clicking "Restore draft" calls onRestore with the draft fields
    - Clicking "Start fresh" calls onStartFresh
    - Malformed JSON in localStorage → component renders null (graceful)
    - After dismiss (either action), component returns null on re-render
    6 it() blocks.
  </behavior>
  <action>
    RED — create `dashboard/tests/draft-recovery-banner.test.tsx`:
    ```tsx
    // @vitest-environment jsdom
    import { describe, it, expect, vi, beforeEach } from "vitest";
    import { render, screen, fireEvent } from "@testing-library/react";
    import { DraftRecoveryBanner } from "@/app/editor/_components/draft-recovery-banner";

    const validDraft = {
      version: 1,
      updatedAt: new Date(Date.now() - 5 * 60_000).toISOString(),
      fields: { name: "Test", slug: "test", runtime: "codex", prompt: "p", schedule: "0 6 * * *", reversibility: "yellow", budget: 40000 },
    };

    describe("DraftRecoveryBanner", () => {
      beforeEach(() => { localStorage.clear(); });

      it("renders null when no draft in localStorage", () => {
        const { container } = render(<DraftRecoveryBanner onRestore={() => {}} onStartFresh={() => {}} />);
        expect(container.innerHTML).toBe("");
      });

      it("renders banner when valid draft present", () => {
        localStorage.setItem("sleepwalker.draft.v1", JSON.stringify(validDraft));
        render(<DraftRecoveryBanner onRestore={() => {}} onStartFresh={() => {}} />);
        expect(screen.getByText(/You have an unsaved draft from/)).toBeTruthy();
        expect(screen.getByText("Restore draft")).toBeTruthy();
        expect(screen.getByText("Start fresh")).toBeTruthy();
      });

      it("calls onRestore with draft fields when Restore draft clicked", () => {
        localStorage.setItem("sleepwalker.draft.v1", JSON.stringify(validDraft));
        const onRestore = vi.fn();
        render(<DraftRecoveryBanner onRestore={onRestore} onStartFresh={() => {}} />);
        fireEvent.click(screen.getByText("Restore draft"));
        expect(onRestore).toHaveBeenCalledTimes(1);
        const arg = onRestore.mock.calls[0][0];
        expect(arg.name).toBe("Test");
        expect(arg.slug).toBe("test");
      });

      it("calls onStartFresh when Start fresh clicked", () => {
        localStorage.setItem("sleepwalker.draft.v1", JSON.stringify(validDraft));
        const onStartFresh = vi.fn();
        render(<DraftRecoveryBanner onRestore={() => {}} onStartFresh={onStartFresh} />);
        fireEvent.click(screen.getByText("Start fresh"));
        expect(onStartFresh).toHaveBeenCalledTimes(1);
      });

      it("renders null when localStorage has malformed JSON", () => {
        localStorage.setItem("sleepwalker.draft.v1", "{bad json");
        const { container } = render(<DraftRecoveryBanner onRestore={() => {}} onStartFresh={() => {}} />);
        expect(container.innerHTML).toBe("");
      });

      it("disappears after user clicks Start fresh", () => {
        localStorage.setItem("sleepwalker.draft.v1", JSON.stringify(validDraft));
        const { container } = render(<DraftRecoveryBanner onRestore={() => {}} onStartFresh={() => {}} />);
        fireEvent.click(screen.getByText("Start fresh"));
        expect(container.innerHTML).toBe("");
      });
    });
    ```

    GREEN — create `dashboard/app/editor/_components/draft-recovery-banner.tsx`:
    ```tsx
    "use client";
    import { useEffect, useState } from "react";

    interface DraftFields {
      name: string; slug: string; runtime: string; prompt: string; schedule: string; reversibility: string; budget: number | string;
    }
    interface StoredDraft {
      version: number;
      updatedAt: string;
      fields: DraftFields;
    }

    interface Props {
      onRestore: (d: DraftFields) => void;
      onStartFresh: () => void;
    }

    const KEY = "sleepwalker.draft.v1";

    function relativeTime(iso: string): string {
      const t = Date.parse(iso);
      if (Number.isNaN(t)) return "recently";
      const mins = Math.max(1, Math.floor((Date.now() - t) / 60_000));
      if (mins < 60) return `${mins} minute${mins === 1 ? "" : "s"} ago`;
      const hours = Math.floor(mins / 60);
      if (hours < 24) return `${hours} hour${hours === 1 ? "" : "s"} ago`;
      const days = Math.floor(hours / 24);
      return `${days} day${days === 1 ? "" : "s"} ago`;
    }

    export function DraftRecoveryBanner({ onRestore, onStartFresh }: Props): JSX.Element | null {
      const [draft, setDraft] = useState<StoredDraft | null>(null);
      const [dismissed, setDismissed] = useState(false);

      useEffect(() => {
        if (typeof window === "undefined") return;
        const raw = localStorage.getItem(KEY);
        if (!raw) return;
        try {
          const parsed = JSON.parse(raw);
          if (parsed && parsed.version === 1 && parsed.fields) {
            setDraft(parsed as StoredDraft);
          }
        } catch {
          // malformed — silently ignore
        }
      }, []);

      if (!draft || dismissed) return null;

      const handleRestore = () => {
        onRestore(draft.fields);
        setDismissed(true);
      };
      const handleStartFresh = () => {
        try { localStorage.removeItem(KEY); } catch {/* ignore */}
        onStartFresh();
        setDismissed(true);
      };

      return (
        <div className="panel-raised border border-aurora-400/30 p-4 mb-6 flex items-center justify-between gap-4">
          <div className="flex flex-col gap-1">
            <p className="text-sm">You have an unsaved draft from {relativeTime(draft.updatedAt)}.</p>
            <p className="text-xs text-moon-400">
              {draft.fields.name || "Unnamed routine"} · {draft.fields.runtime || "no runtime"} · {draft.fields.slug || "no slug"}
            </p>
          </div>
          <div className="flex gap-2">
            <button type="button" className="btn-ghost text-aurora-400" onClick={handleRestore}>Restore draft</button>
            <button type="button" className="btn-ghost" onClick={handleStartFresh}>Start fresh</button>
          </div>
        </div>
      );
    }
    ```

    Run `pnpm test draft-recovery-banner` → pass. typecheck clean.
    Commit: `feat(03-06): add DraftRecoveryBanner + 6 jsdom tests`.
  </action>
  <verify>
    <automated>cd dashboard && pnpm test draft-recovery-banner.test.tsx 2>&1 | grep -E "Tests\s+\d+\s+passed"</automated>
    <automated>cd dashboard && pnpm typecheck 2>&1 | grep -c "error TS" || echo 0</automated>
    <automated>grep -c "You have an unsaved draft from" dashboard/app/editor/_components/draft-recovery-banner.tsx</automated>
    <automated>grep -c "Restore draft\|Start fresh" dashboard/app/editor/_components/draft-recovery-banner.tsx</automated>
  </verify>
  <done>
    DraftRecoveryBanner reads localStorage, renders banner only when valid draft present, handles malformed JSON gracefully, dismisses on either action. 6 tests green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: PreviewPanel (sticky right column) — no test, covered by editor-client integration in 03-07</name>
  <read_first>
    - .planning/phases/03-editor/03-UI-SPEC.md §Grid (lines 220-225) + §Live-preview copy (lines 156-163)
    - .planning/phases/03-editor/03-PATTERNS.md §preview-panel.tsx (lines 503-515)
    - .planning/phases/02-adapters/02-CONTEXT.md lines 32-57 (assertValidSlug throws — wrap calls in try/catch)
    - dashboard/lib/runtime-adapters/slug.ts (find toBundleDir, toPlistPath, toMarkerTag exports; check which exist and exact signatures)
  </read_first>
  <files>dashboard/app/editor/_components/preview-panel.tsx</files>
  <action>
    Verify builder exports before writing code:
    `grep -E "^export function to(BundleDir|PlistPath|MarkerTag)" dashboard/lib/runtime-adapters/slug.ts`
    If any builder is missing from slug.ts public API, fall back to raw string templates for that line. Document the fallback in a comment.

    Create `dashboard/app/editor/_components/preview-panel.tsx`:
    ```tsx
    "use client";
    import type { HealthStatus, Runtime } from "@/lib/runtime-adapters/types";
    import { CronPreview } from "./cron-preview";

    interface Props {
      runtime: Runtime | "";
      slug: string;
      schedule: string;
      healthStatus: HealthStatus | null;
    }

    function safe<T>(fn: () => T, fallback: T): T {
      try { return fn(); } catch { return fallback; }
    }

    function bundlePath(runtime: Runtime | "", slug: string): string {
      if (!runtime || !slug) return "—";
      const root = ({
        "claude-routines": "routines-cloud",
        "claude-desktop":  "routines-local",
        "codex":           "routines-codex",
        "gemini":          "routines-gemini",
      } as const)[runtime];
      return `${root}/${slug}/`;
    }

    function plistPath(runtime: Runtime | "", slug: string): string | null {
      if (runtime !== "codex" && runtime !== "gemini") return null;
      if (!slug) return null;
      return safe(() => `~/Library/LaunchAgents/com.sleepwalker.${runtime}.${slug}.plist`, "—");
    }

    function markerTag(runtime: Runtime | "", slug: string): string {
      if (!runtime || !slug) return "—";
      return `[sleepwalker:${runtime}/${slug}]`;
    }

    export function PreviewPanel({ runtime, slug, schedule, healthStatus }: Props): JSX.Element {
      return (
        <aside className="w-80 sticky top-10 panel p-4 flex flex-col gap-3">
          <div>
            <div className="label mb-1">BUNDLE PATH</div>
            <div className="data text-xs text-moon-400">{bundlePath(runtime, slug)}</div>
          </div>
          {plistPath(runtime, slug) && (
            <div>
              <div className="label mb-1">PLIST PATH</div>
              <div className="data text-xs text-moon-600">{plistPath(runtime, slug)}</div>
            </div>
          )}
          <div>
            <div className="label mb-1">MARKER TAG</div>
            <div className="data text-xs text-moon-600">{markerTag(runtime, slug)}</div>
          </div>
          <div>
            <div className="label mb-1">SCHEDULE</div>
            <CronPreview expression={schedule} />
          </div>
          {healthStatus && (
            <div>
              <div className="label mb-1">HEALTH</div>
              <div className="text-xs">
                {healthStatus.available
                  ? <span className="pill-green">Ready</span>
                  : <span className="pill-amber">{healthStatus.reason ?? "Unavailable"}</span>}
              </div>
            </div>
          )}
        </aside>
      );
    }
    ```

    NOTE: Using a local runtime→root map instead of importing toBundleDir because toBundleDir throws on invalid slug (per Phase 2 CONTEXT.md line 43), and PreviewPanel must render mid-keystroke safely. The `safe()` wrapper is kept on plistPath for consistency even though the current template is string-interp only — future-proofing for when a real builder is wired.

    Run `pnpm typecheck` → clean.
    Commit: `feat(03-06): add PreviewPanel sticky right column`.
  </action>
  <verify>
    <automated>grep -c "sticky top-10" dashboard/app/editor/_components/preview-panel.tsx</automated>
    <automated>grep -c "sleepwalker:" dashboard/app/editor/_components/preview-panel.tsx</automated>
    <automated>cd dashboard && pnpm typecheck 2>&1 | grep -c "error TS" || echo 0</automated>
    <automated>cd dashboard && pnpm test 2>&1 | grep -E "Tests\s+\d+\s+passed"</automated>
  </verify>
  <done>
    PreviewPanel renders bundle path + plist path (local runtimes only) + marker tag + cron preview + health status; safely handles invalid slug mid-typing via local string-builder (not toBundleDir). All existing tests still green.
  </done>
</task>

</tasks>

<verification>
- Full test suite green after this plan (should be ~125 tests = 84 from prior plans + 6 + 4 + 6 + others still passing)
- `/editor` route resolvable (page.tsx + editor-client stub + 5 subcomponents all present)
- All 4 UI-SPEC runtime titles present in runtime-radio-grid.tsx
- Exact UI-SPEC error strings verified via grep
- v0.1 frozen surface untouched
</verification>

<success_criteria>
- [ ] /editor page.tsx renders PageHeader with exact UI-SPEC strings
- [ ] editor-client.tsx stub keeps typecheck clean (Plan 03-07 replaces)
- [ ] RuntimeRadioGrid: 4 cards, unavailable dim, selected ring, warning amber pill
- [ ] CronPreview: valid shows "Runs …" in aurora; invalid shows UI-SPEC red message
- [ ] SecretScanPanel: exact heading "Secret detected — save blocked" + fix example + optional scroll link
- [ ] DraftRecoveryBanner: reads localStorage, ignores malformed JSON, dismisses on action
- [ ] PreviewPanel: sticky right column, safe handling of mid-typing invalid slug
- [ ] jsdom working for 3 .test.tsx files
- [ ] Test count grows by ≥ 16 blocks
</success_criteria>

<output>
After completion, write `.planning/phases/03-editor/03-06-SUMMARY.md`:
- 9 files created/modified (page.tsx + stub editor-client + 5 subcomponents + 3 test.tsx)
- Commit SHA(s)
- Test count delta
- Cross-ref 03-VALIDATION.md rows 2, 3, 13 now satisfied
</output>
