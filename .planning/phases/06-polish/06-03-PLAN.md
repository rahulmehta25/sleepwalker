---
phase: 06-polish
plan: 03
type: execute
wave: 1
depends_on: [06-01, 06-02]
files_modified:
  - docs/AUTHORING.md
  - README.md
autonomous: true
requirements: [DOCS-01]
tags: [docs, authoring, oss, troubleshooting, mac-sleep]
must_haves:
  truths:
    - "A new user who just cloned the repo can reach first custom routine in under 10 minutes by following docs/AUTHORING.md"
    - "docs/AUTHORING.md covers all 4 runtimes (Claude Routines, Claude Desktop, Codex, Gemini) with auth prereqs and quirks"
    - "Mac-sleep caveat with caffeinate + pmset + launchctl print recipes is explicitly documented in §4"
    - "Troubleshooting section is indexed by error message string so users can grep their exact error"
    - "README.md links to docs/AUTHORING.md so discovery path from landing page exists"
    - "Each §3 runtime subsection links to its matching templates/routine-<runtime>.md file from Plan 06-01"
  artifacts:
    - path: "docs/AUTHORING.md"
      provides: "7-section authoring walkthrough"
      min_lines: 600
    - path: "README.md"
      provides: "discovery link to AUTHORING.md"
      contains: "AUTHORING.md"
  key_links:
    - from: "docs/AUTHORING.md §1 Quick Start"
      to: "docs/QUICKSTART.md"
      via: "markdown link"
      pattern: "QUICKSTART\\.md"
    - from: "docs/AUTHORING.md §3 The Four Runtimes"
      to: "templates/routine-*.md (from Plan 06-01)"
      via: "markdown links (one per runtime)"
      pattern: "templates/routine-"
    - from: "docs/AUTHORING.md §1 Quick Start"
      to: "/diagnostics"
      via: "prereq-check pointer"
      pattern: "/diagnostics"
---

<objective>
Ship `docs/AUTHORING.md` — the single user-facing markdown file that walks an OSS stranger from "just cloned the repo" to "first custom routine running on any of 4 runtimes" in under 10 minutes, with a Troubleshooting table indexed by exact error message string.

Purpose: DOCS-01 is the primary OSS artifact for v0.2. Without it, every new user has to reverse-engineer the editor + four runtimes + Mac-sleep quirks + TCC bundle staging + flock preflight from scratch. The Troubleshooting section is the single highest-value design decision because Phase 2 smoke proved that undocumented launchd/TCC errors cost 30+ min debug sessions each.

Output: `docs/AUTHORING.md` (600-1000 lines, 7 sections in locked order per D-01) + a single additive link in `README.md` to aid discovery. Depends on Plan 06-01 because §3 cross-links to the four `templates/routine-<runtime>.md` files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-polish/06-CONTEXT.md
@.planning/phases/06-polish/06-RESEARCH.md
@.planning/phases/06-polish/06-PATTERNS.md
@CLAUDE.md
@docs/QUICKSTART.md
@docs/ROUTINES.md
@docs/ARCHITECTURE.md
@README.md

<interfaces>
<!-- Concrete strings and values the executor MUST use verbatim. -->

From 06-CONTEXT.md §DOCS-01 (LOCKED 7-section order — DO NOT re-order):
  1. Quick Start
  2. Author a Custom Routine
  3. The Four Runtimes
  4. Scheduling & Mac Sleep
  5. Safety Semantics
  6. Troubleshooting
  7. Going Further

From 06-RESEARCH.md §1.2 — GitHub anchor targets (preserve these slug strings):
  #quick-start / #author-a-custom-routine / #open-editor / #fill-the-form /
  #click-save / #click-deploy / #the-four-runtimes / #claude-routines /
  #claude-desktop / #codex / #gemini / #scheduling-and-mac-sleep /
  #cron-syntax / #mac-must-be-awake / #launchctl-print / #safety-semantics /
  #reversibility / #defer-policies / #approximate-budget / #troubleshooting /
  #going-further

From 06-RESEARCH.md §1.3 — 13-row Troubleshooting table (MUST be a table, grep-friendly, exact error strings):
  Error column values (LITERAL, copy-pasteable by users):
    `ERROR: jq is required but not installed. Install with: brew install jq`
    `ERROR: flock is required but not installed. Install with: brew install flock`
    `"event":"failed","preview":"..Operation not permitted.."`
    `"event":"failed","preview":"..Not inside a trusted directory and --skip-git-repo-check was not specified.."`
    Claude Desktop routine never fires even after save
    `"event":"budget_exceeded","preview":"Stopped at N chars (budget: M, approximate)"`
    `"event":"deferred","preview":"red+balanced"` / `"red+strict"` / `"yellow+strict"`
    `Gemini quota project not configured. Set runtime_config.gemini_quota_project in config.json.`
    "codex not in PATH" red pill on landing page
    "bundle not found" in audit.jsonl
    Secret-scan blocks save with "Prompt appears to contain a secret (... at line N, column M). Replace with ${VAR}..."
    Save rejected with "Slug must match `^[a-z][a-z0-9-]{0,63}$` ..."
    Save rejected with "A <runtime> routine with slug <slug> exists. Slugs must be unique across runtimes."
    Dashboard says `cloud queue inactive · configure GitHub in Settings`

From 06-RESEARCH.md §1.4 — Three Mac-sleep mitigation patterns (document all three):
  Pattern A — caffeinate (interactive dev):
    caffeinate -i &                              # indefinite
    caffeinate -i -t 28800 &                     # 8 hours
    caffeinate -i -w $(pgrep -f "next dev")      # until pid exits
  Pattern B — pmset schedule (scheduled wake overnight):
    sudo pmset schedule wake "MM/DD/YY HH:MM:SS"
    sudo pmset repeat wake MTWRFSU 03:00:00
    pmset -g sched
    sudo pmset schedule cancelall
  Pattern C — launchctl print (debug why job didn't fire):
    launchctl print gui/$UID/com.sleepwalker.codex.morning-brief

From CLAUDE.md §Conventions — marker tag + launchd label (use verbatim in examples):
  [sleepwalker:<runtime>/<slug>]   # v0.2 form, runtime prefix required
  com.sleepwalker.<runtime>.<slug> # launchd label

Template files produced by Plan 06-01 (reference from §3):
  templates/routine-claude-routines.md
  templates/routine-claude-desktop.md
  templates/routine-codex.md
  templates/routine-gemini.md

From 06-RESEARCH.md §1.5 — Length target:
  600-1000 lines (3000-5000 words, 10-minute read)

From 06-RESEARCH.md §2.1 — Reversibility colors + policies (for §5):
  Colors: green | yellow | red
  Policies: conservative | balanced | aggressive
  Budget unit: "chars (approximate)" — NEVER "tokens" (SAFE-01 invariant)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/AUTHORING.md with 7 locked sections and 13-row Troubleshooting table</name>
  <read_first>
    - docs/QUICKSTART.md (existing v0.1 doc — AUTHORING.md complements, does not replace; do NOT duplicate §1-2)
    - docs/ROUTINES.md (14 v0.1 routine catalog — reference from §7 Going Further)
    - docs/ARCHITECTURE.md (v0.1 internals — reference from §7)
    - templates/routine-claude-routines.md (produced by Plan 06-01 — §3.1 links here)
    - templates/routine-claude-desktop.md (produced by Plan 06-01 — §3.2 links here)
    - templates/routine-codex.md (produced by Plan 06-01 — §3.3 links here)
    - templates/routine-gemini.md (produced by Plan 06-01 — §3.4 links here)
    - .planning/phases/06-polish/06-CONTEXT.md §DOCS-01 (LOCKED 7-section structure)
    - .planning/phases/06-polish/06-RESEARCH.md §1.1-1.6 (outline + Troubleshooting rows + Mac-sleep patterns)
    - .planning/phases/06-polish/06-PATTERNS.md §docs/AUTHORING.md (QUICKSTART.md tone + structure analog)
    - .planning/phases/02-adapters/02-SUMMARY.md (Q1 Claude Desktop manual-add + TCC bundle staging + --skip-git-repo-check context for Troubleshooting entries)
    - CLAUDE.md (frozen v0.1 surface — do NOT describe v0.1 behavior as different from what ships today)
  </read_first>
  <action>
Create `docs/AUTHORING.md` as a single markdown file (600-1000 lines target per D-01). Follow the exact 7-section order below with the exact H2 headings. Every heading becomes a GitHub-rendered anchor; preserve anchor slugs exactly as written.

**File header:**
```markdown
# Sleepwalker — Authoring Guide

Ship a custom routine to any of 4 runtimes in under 10 minutes.

> **Prereq check:** If you're unsure whether `jq`, `flock`, `claude`, `codex`, or `gemini` are installed, open <http://localhost:4001/diagnostics> after `pnpm dev --port 4001`. Every required dependency has a pass/fail row and a copy-to-GitHub-issue button.

> **New to Sleepwalker?** Run through [QUICKSTART.md](./QUICKSTART.md) §1-3 first — this guide picks up at §2 Author a Custom Routine with v0.2's multi-runtime `/editor` flow.

---
```

**§1. Quick Start** (anchor `#quick-start`): 3-minute path — `git clone` + `./install.sh` + `pnpm dev --port 4001` + visit `localhost:4001`, expect 14 v0.1 routines + 4 runtime health badges. Reference QUICKSTART.md §1-2 for v0.1 install details; DO NOT duplicate. Mention the Phase 5 `flock` preflight and point readers at §6 Troubleshooting if the preflight fails.

**§2. Author a Custom Routine** (anchor `#author-a-custom-routine`) with §2.1 Open /editor, §2.2 Fill the form, §2.3 Click Save, §2.4 Click Deploy. Walk through the editor form fields in order: name, slug, runtime radio grid, prompt textarea, schedule (with cronstrue preview), reversibility pills (green/yellow/red), budget (labeled "chars (approximate)", NEVER "tokens"). Show how Save writes to `routines-<runtime>/<slug>/` and Deploy drives the state machine `planning → writing → loading → verified`. Mention per-routine secret-scan rejection on `sk_live_`/`ghp_`/40-char-hex and point to §6 Troubleshooting for the exact error strings. Mention the four pre-authored templates from Plan 06-01 as copy-paste starting points (one-line link list).

**§3. The Four Runtimes** (anchor `#the-four-runtimes`) with 4 subsections in this order:
  - §3.1 Claude Code Routines (anchor `#claude-routines`): cloud-based. Auth: Claude Code subscription. Invocation: schedule triggers via `/schedule create` browser handoff; API triggers also require browser handoff (NO full programmatic creation per CLAUDE.md §Non-existence facts). Link: `[Template](../templates/routine-claude-routines.md)`.
  - §3.2 Claude Code Desktop Scheduled Tasks (anchor `#claude-desktop`): local Mac. Auth: signed-in Claude Desktop app. **CRITICAL QUIRK (Q1 from Phase 2 smoke):** Claude Desktop 1.3109.0 does NOT watch `~/.claude/scheduled-tasks/`. SKILL.md must be copied manually into Desktop's Schedule tab. The editor offers a "Copy SKILL.md" button for this workflow. Link: `[Template](../templates/routine-claude-desktop.md)`.
  - §3.3 OpenAI Codex Pro (anchor `#codex`): local CLI. Auth: `codex auth login`. Sleepwalker shells out to `codex` via `bin/sleepwalker-run-cli` supervisor. Quirk: `--skip-git-repo-check` required (appended automatically post-commit `633a07a`); bundle staged to `~/.sleepwalker/staged-bundles/codex/<slug>/` to avoid TCC sandbox. Link: `[Template](../templates/routine-codex.md)`.
  - §3.4 Google Gemini CLI Pro (anchor `#gemini`): local CLI. Auth: `gcloud auth login` with quota project. Sleepwalker refuses to deploy without explicit `runtime_config.gemini_quota_project` in config.json (prevents silent wrong-project billing). `healthCheck()` displays active auth mode + quota project. Link: `[Template](../templates/routine-gemini.md)`.

Each subsection: what it is (1 para), auth prereqs (bullet list), how Sleepwalker invokes it (1 para), quirks/gotchas (bullet list), link to matching template file.

**§4. Scheduling & Mac Sleep** (anchor `#scheduling-and-mac-sleep`) with §4.1 Cron syntax refresher, §4.2 Mac must be awake, §4.3 Debug with launchctl print.
  - §4.1: cron-5 fields, examples `*/5 * * * *`, `0 3 * * *`, `0 7 * * 1-5`. Point at editor's cronstrue live preview.
  - §4.2: LOAD-BEARING callout "**Sleepwalker's CLI runtimes (Codex, Gemini) run via launchd, which does not fire while your Mac is sleeping or closed.**" Then present Patterns A / B / C (caffeinate / pmset / launchctl print) as three fenced code blocks with the exact commands from <interfaces> above.
  - §4.3: The `launchctl print gui/$UID/com.sleepwalker.codex.morning-brief` recipe with the exact key fields to look for (state / last exit code / pending jobs / program / working directory). Mention that if `working directory` shows the repo path instead of `~/.sleepwalker/staged-bundles/...`, the plist is pre-Plan 02-12 and needs redeploy.

**§5. Safety Semantics** (anchor `#safety-semantics`) with §5.1 Reversibility colors, §5.2 Defer policies, §5.3 Approximate budget cap.
  - §5.1: `green` = safe to auto-approve / `yellow` = needs queue review / `red` = irreversible; relate to hook `sleepwalker-defer-irreversible.sh`.
  - §5.2: conservative / balanced / aggressive; per-fleet override via `~/.sleepwalker/settings.json`.
  - §5.3: Budgets are **approximate character counts (±40%)**, NEVER tokens. Cite SAFE-01. Raise budget by 50% to give headroom if `event:"budget_exceeded"` fires.

**§6. Troubleshooting** (anchor `#troubleshooting`) — a single markdown table with columns: `Error message | Cause | Fix | Reference`. Populate ALL 13 rows verbatim from <interfaces> above (06-RESEARCH.md §1.3). Use backticks around error strings so users copy-pasting stack traces into Cmd-F hit a clean match.

**§7. Going Further** (anchor `#going-further`): link to `docs/ARCHITECTURE.md` (v0.1 + v0.2 internals), `docs/ROUTINES.md` (14 v0.1 catalog), `.github/workflows/ci.yml` (CI surface, produced by Plan 06-06), GitHub issues. Add a 2-line note: "If you changed `dashboard/lib/bundle-schema.ts` or `dashboard/app/editor/**`, please also update this guide — docs drift fast otherwise."

**Tone/voice (per 06-PATTERNS.md §docs/AUTHORING.md):** second-person, short sentences, no marketing fluff. Mirror QUICKSTART.md's `## N. Title` numbering + fenced `bash` blocks + `---` hr separators.

**After writing AUTHORING.md, amend `README.md`:** append a single additive entry under the existing Documentation / Docs section pointing to `docs/AUTHORING.md` with a one-line description. Do NOT reorganize README.md; single-line insertion only.
  </action>
  <verify>
    <automated>test $(wc -l < docs/AUTHORING.md) -ge 600 && test $(wc -l < docs/AUTHORING.md) -le 1100 && grep -c "^## " docs/AUTHORING.md | awk '{exit ($1 >= 7 ? 0 : 1)}' && grep -qE "^## 1\. Quick Start" docs/AUTHORING.md && grep -qE "^## 2\. Author a Custom Routine" docs/AUTHORING.md && grep -qE "^## 3\. The Four Runtimes" docs/AUTHORING.md && grep -qE "^## 4\. Scheduling & Mac Sleep" docs/AUTHORING.md && grep -qE "^## 5\. Safety Semantics" docs/AUTHORING.md && grep -qE "^## 6\. Troubleshooting" docs/AUTHORING.md && grep -qE "^## 7\. Going Further" docs/AUTHORING.md && grep -q "caffeinate -i" docs/AUTHORING.md && grep -q "pmset schedule wake" docs/AUTHORING.md && grep -q "launchctl print gui" docs/AUTHORING.md && grep -q "chars (approximate)" docs/AUTHORING.md && ! grep -qiE "budget.*tokens|tokens.*budget" docs/AUTHORING.md && grep -q "AUTHORING.md" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `docs/AUTHORING.md` has ≥600 and ≤1100 lines
    - `docs/AUTHORING.md` contains exactly these 7 top-level H2 headings in order: `## 1. Quick Start`, `## 2. Author a Custom Routine`, `## 3. The Four Runtimes`, `## 4. Scheduling & Mac Sleep`, `## 5. Safety Semantics`, `## 6. Troubleshooting`, `## 7. Going Further`
    - `grep -c '^### ' docs/AUTHORING.md` returns at least 10 (sub-sections present for §2, §3, §4, §5)
    - `grep -c 'caffeinate' docs/AUTHORING.md` returns ≥ 1 (Mac-sleep §4.2)
    - `grep -c 'pmset' docs/AUTHORING.md` returns ≥ 1 (Mac-sleep §4.2)
    - `grep -c 'launchctl print' docs/AUTHORING.md` returns ≥ 1 (§4.3 debug recipe)
    - `grep -c 'chars (approximate)' docs/AUTHORING.md` returns ≥ 1 (§5.3 SAFE-01 honest label)
    - `grep -iE 'budget.*tokens|tokens.*budget' docs/AUTHORING.md` returns zero matches (SAFE-01 negative invariant)
    - §6 Troubleshooting contains a markdown table with at least 13 data rows: `awk '/^## 6\\. Troubleshooting/,/^## 7\\. Going Further/' docs/AUTHORING.md | grep -c '^|' >= 14` (header + 13 rows minimum)
    - §6 contains literal strings: `ERROR: jq is required but not installed`, `ERROR: flock is required but not installed`, `Operation not permitted`, `--skip-git-repo-check`, `budget_exceeded`, `gemini_quota_project`, `ghp_`, `sk_live_`
    - §3 contains exactly 4 H3 subsections: `### 3.1`, `### 3.2`, `### 3.3`, `### 3.4` (one per runtime)
    - §3 cross-references all 4 templates: `grep -c 'templates/routine-' docs/AUTHORING.md >= 4`
    - §1 references QUICKSTART.md: `grep -c 'QUICKSTART.md' docs/AUTHORING.md >= 1`
    - README.md contains a link to AUTHORING.md: `grep -c 'AUTHORING.md' README.md >= 1`
    - No h1 headings after the first line: `grep -c '^# ' docs/AUTHORING.md` equals 1
    - All 4 runtime strings appear at least once: `claude-routines`, `claude-desktop`, `codex`, `gemini`
  </acceptance_criteria>
  <done>
    docs/AUTHORING.md exists at 600-1000 lines with the locked 7-section order, the 13-row Troubleshooting table with grep-friendly error strings, Mac-sleep §4.2 with all three caffeinate/pmset/launchctl patterns, §5.3 honest "chars (approximate)" budget labeling, §3 covering all 4 runtimes with links to their templates (from Plan 06-01), and README.md amended with a single additive link. The SAFE-01 negative invariant (no "tokens" near "budget") holds. Single `docs(06-03):` commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → user's Mac | User follows commands from AUTHORING.md verbatim; any malicious `sudo` command in §4 would execute |
| README → AUTHORING | Plain markdown link; no script execution |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | T (Tampering) | §4.2 `sudo pmset` commands | mitigate | Use exact `sudo pmset schedule wake` / `sudo pmset repeat wake` syntax verified from installed `man pmset` (06-RESEARCH §1.4); no piped curl, no `bash <(...)` pattern in any example |
| T-06-03-02 | I (Information disclosure) | Troubleshooting examples | mitigate | Error-string examples use generic placeholders (`<slug>`, `<you>`); no real PAT, API key, or personal filesystem path in any code block |
| T-06-03-03 | D (Denial of service) | Mac-sleep `caffeinate -i` indefinite | accept | User-initiated, documented side effect ("keeps Mac awake indefinitely"); user can `kill %1` at any time |
| T-06-03-04 | R (Repudiation) | Links to phase commits (`633a07a`, `4cbb5bb`) | accept | Read-only git-commit references; cannot be abused by readers |
</threat_model>

<verification>
Run after task complete:
  - `test $(wc -l < docs/AUTHORING.md) -ge 600` (length floor)
  - `grep -c '^## ' docs/AUTHORING.md` equals 7 (seven top-level sections)
  - `awk '/^## 6\. Troubleshooting/,/^## 7\. Going Further/' docs/AUTHORING.md | grep -c '^|'` ≥ 14
  - `grep -iE 'budget.*tokens|tokens.*budget' docs/AUTHORING.md` produces zero hits
  - `grep 'AUTHORING.md' README.md` produces at least one hit
  - Manual spot-check: open docs/AUTHORING.md in a markdown renderer and confirm all anchor slugs `#quick-start`, `#claude-desktop`, `#troubleshooting`, etc. resolve to the expected headings.
</verification>

<success_criteria>
- `docs/AUTHORING.md` committed at 600-1000 lines with all 7 locked sections in order
- README.md links to AUTHORING.md (single additive line)
- Troubleshooting §6 is a table with ≥13 rows, each with a literal error string in column 1
- Mac-sleep §4.2 contains all three caffeinate/pmset/launchctl patterns with exact commands from 06-RESEARCH
- SAFE-01 invariant: zero co-occurrence of "budget" and "tokens" in AUTHORING.md
- All four runtimes (`claude-routines`, `claude-desktop`, `codex`, `gemini`) named in §3 and linked to their templates
- DOCS-01 requirement in `.planning/REQUIREMENTS.md` can be flipped from Pending to Complete at phase seal
</success_criteria>

<output>
After completion, create `.planning/phases/06-polish/06-03-SUMMARY.md` documenting:
- Final line count + section count
- Troubleshooting row count
- List of anchor slugs (for cross-reference by subsequent plans / docs drift reviewers)
- Any Rule 1/2/3 auto-fixes applied
- Whether README.md amendment was a single-line addition or required a section rewrite (should be single-line)
</output>
