---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - routines-codex/.gitkeep
  - routines-gemini/.gitkeep
  - templates/.gitkeep
  - docs/activity_log.md
autonomous: true
requirements:
  - ADPT-02
must_haves:
  truths:
    - "routines-codex/ directory exists and is tracked by git"
    - "routines-gemini/ directory exists and is tracked by git"
    - "templates/ directory exists and is tracked by git"
    - "Each .gitkeep contains a protective comment so later phases do not inadvertently delete it"
    - "All 14 v0.1 routine paths (routines-local/, routines-cloud/) are byte-identical — zero modification"
  artifacts:
    - path: "routines-codex/.gitkeep"
      provides: "Git-tracked marker establishing routines-codex/ as a sibling to routines-local/ and routines-cloud/"
      contains: "Placeholder"
    - path: "routines-gemini/.gitkeep"
      provides: "Git-tracked marker establishing routines-gemini/ as a sibling directory for Gemini CLI Pro routine bundles"
      contains: "Placeholder"
    - path: "templates/.gitkeep"
      provides: "Git-tracked marker establishing templates/ for Phase 6 runtime templates"
      contains: "Placeholder"
  key_links:
    - from: "Repository root"
      to: "routines-codex/, routines-gemini/, templates/ as siblings of routines-local/, routines-cloud/"
      via: "mkdir + .gitkeep in each"
      pattern: "routines-(codex|gemini)/\\.gitkeep"
---

<objective>
Create the three new root-level directories that Phase 2 adapters and Phase 6 templates will populate: `routines-codex/`, `routines-gemini/`, and `templates/`. Each contains a `.gitkeep` file with a protective inline comment (per RESEARCH.md §Pitfall 2) so empty directories are tracked by git and survive later phases.

This is the filesystem side of ADPT-02 — the naming convention insists these parallel-sibling directories exist alongside v0.1's `routines-local/` and `routines-cloud/`. No changes to v0.1 directories. No source code. No dependencies.

Purpose: Unblock Phase 2 adapters (codex.ts, gemini.ts will write bundles into these directories) and Phase 6 templates (per-runtime starter files go into templates/).
Output: 3 `.gitkeep` files + 1 activity log entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
@CLAUDE.md

<frozen_surface>
MUST NOT TOUCH — Plan 02 only CREATES three new sibling directories plus activity_log:
- routines-local/** (6 v0.1 routines)
- routines-cloud/** (9 v0.1 cloud routines including _test-zen)
- Any file in install.sh, hooks/, bin/, dashboard/lib/*.ts, dashboard/app/**
</frozen_surface>

<rationale>
Per RESEARCH.md §Pitfall 2 (lines 474-478):
"Add a one-line comment in the phase plan that every `.gitkeep` contains a single line:
`# Placeholder — do not delete; empty dirs are not tracked by git`"

This ensures later phases (especially Phase 3 editor that may write the first real bundle
into routines-codex/) recognize the .gitkeep as intentional scaffolding, not legacy.

Per RESEARCH.md §System Architecture Diagram (lines 194-213), `templates/` is a NEW root
directory introduced in Phase 1 for Phase 6 consumption (per-runtime template files).
</rationale>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create routines-codex/.gitkeep</name>
  <files>routines-codex/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §Pitfall 2 lines 474-478
    - .planning/phases/01-foundation/01-VALIDATION.md Task 1-03-01
    - CLAUDE.md §Conventions (verify routines-codex/ sibling naming)
  </read_first>
  <action>
Create the directory `routines-codex/` at the repository root (sibling of `routines-local/` and `routines-cloud/`) and create the file `routines-codex/.gitkeep` with EXACTLY this content (one line plus trailing newline):

```
# Placeholder -- do not delete; empty dirs are not tracked by git. Phase 2 adapters (codex.ts) write real bundles into sibling subdirs of this directory.
```

The file contains ONE line. No blank lines. No additional content.

Why this exact comment (per RESEARCH.md §Pitfall 2): `.gitkeep` is a git convention, not a built-in. Developers unfamiliar with the pattern sometimes delete "empty" files. The inline comment documents intent so Phase 3/6 contributors preserve it.

DO NOT:
- Create `README.md` or `.keep` as the placeholder — `.gitkeep` is the canonical OSS idiom (RESEARCH.md "Don't Hand-Roll" table, last row).
- Add multiple lines or extra content — a single comment line is the entire file.
- Touch `routines-local/` or `routines-cloud/` — they are v0.1 frozen surface.
- Create any files inside `routines-codex/` other than `.gitkeep`.
  </action>
  <verify>
    <automated>test -d /Users/rahulmehta/Desktop/Projects/sleepwalker/routines-codex && test -f /Users/rahulmehta/Desktop/Projects/sleepwalker/routines-codex/.gitkeep && grep -c "Placeholder" /Users/rahulmehta/Desktop/Projects/sleepwalker/routines-codex/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - `test -d routines-codex` exits 0
    - `test -f routines-codex/.gitkeep` exits 0
    - `wc -l routines-codex/.gitkeep` returns `1` (one line with trailing newline)
    - `grep -c "Placeholder" routines-codex/.gitkeep` returns `1`
    - `ls routines-codex/` shows only `.gitkeep` (no other files)
    - `git status routines-codex/.gitkeep` shows file as untracked or staged (not ignored)
  </acceptance_criteria>
  <done>routines-codex/ directory exists, tracked via .gitkeep with protective comment.</done>
</task>

<task type="auto">
  <name>Task 2: Create routines-gemini/.gitkeep</name>
  <files>routines-gemini/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §Pitfall 2 lines 474-478
    - .planning/phases/01-foundation/01-VALIDATION.md Task 1-03-01
  </read_first>
  <action>
Create the directory `routines-gemini/` at the repository root (sibling of `routines-local/`, `routines-cloud/`, `routines-codex/`) and create `routines-gemini/.gitkeep` with EXACTLY this content (one line):

```
# Placeholder -- do not delete; empty dirs are not tracked by git. Phase 2 adapters (gemini.ts) write real bundles into sibling subdirs of this directory.
```

Why: Per ADPT-02 + RESEARCH.md §Pattern 2 (Naming Convention), `toBundleDir("gemini", slug)` returns `routines-gemini/<slug>`. The directory must pre-exist for Phase 2 to be able to resolve that path without `mkdir -p` racing.

DO NOT:
- Substitute `gemini-cli/` or any alternative name — must be exactly `routines-gemini/` per CLAUDE.md Conventions and toBundleDir() builder spec.
- Create any subdirectories inside `routines-gemini/` — first real subdir is written by Phase 3 editor.
  </action>
  <verify>
    <automated>test -d /Users/rahulmehta/Desktop/Projects/sleepwalker/routines-gemini && test -f /Users/rahulmehta/Desktop/Projects/sleepwalker/routines-gemini/.gitkeep && grep -c "Placeholder" /Users/rahulmehta/Desktop/Projects/sleepwalker/routines-gemini/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - `test -d routines-gemini` exits 0
    - `test -f routines-gemini/.gitkeep` exits 0
    - `wc -l routines-gemini/.gitkeep` returns `1`
    - `grep -c "Placeholder" routines-gemini/.gitkeep` returns `1`
    - `ls routines-gemini/` shows only `.gitkeep`
  </acceptance_criteria>
  <done>routines-gemini/ directory exists, tracked via .gitkeep with protective comment.</done>
</task>

<task type="auto">
  <name>Task 3: Create templates/.gitkeep</name>
  <files>templates/.gitkeep</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md §System Architecture Diagram lines 208-211 (templates/ directory)
    - .planning/phases/01-foundation/01-VALIDATION.md Task 1-03-01
    - .planning/REQUIREMENTS.md DOCS-02 (four runtime templates live here, Phase 6)
  </read_first>
  <action>
Create the directory `templates/` at the repository root (sibling of routines-*/ directories) and create `templates/.gitkeep` with EXACTLY this content:

```
# Placeholder -- do not delete; empty dirs are not tracked by git. Phase 6 (DOCS-02) writes four runtime template files here: routine-claude-routines.md, routine-claude-desktop.md, routine-codex.md, routine-gemini.md.
```

Single line, trailing newline.

Why: Per REQUIREMENTS.md DOCS-02 and ROADMAP.md Phase 6 success criterion #2, four `templates/routine-*.md` files ship in Phase 6. The directory must exist before then so Phase 6 writers don't need to create it.

DO NOT:
- Create placeholder template .md files for the four runtimes now — those are Phase 6 work.
- Name the directory `.templates/` or `routine-templates/` — must be exactly `templates/`.
  </action>
  <verify>
    <automated>test -d /Users/rahulmehta/Desktop/Projects/sleepwalker/templates && test -f /Users/rahulmehta/Desktop/Projects/sleepwalker/templates/.gitkeep && grep -c "Placeholder" /Users/rahulmehta/Desktop/Projects/sleepwalker/templates/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - `test -d templates` exits 0
    - `test -f templates/.gitkeep` exits 0
    - `wc -l templates/.gitkeep` returns `1`
    - `grep -c "Placeholder" templates/.gitkeep` returns `1`
    - `ls templates/` shows only `.gitkeep`
  </acceptance_criteria>
  <done>templates/ directory exists, tracked via .gitkeep with protective comment naming Phase 6 templates.</done>
</task>

<task type="auto">
  <name>Task 4: Append activity log entry for Plan 02</name>
  <files>docs/activity_log.md</files>
  <read_first>
    - docs/activity_log.md (verify format of most recent entry; append after)
    - /Users/rahulmehta/.claude/CLAUDE.md §Activity Log
  </read_first>
  <action>
Append a new entry at the END of `docs/activity_log.md`. Separate from any prior entry with one blank line. Format per global CLAUDE.md:

```
## 2026-04-18 HH:MM EST

### User Prompt
"gsd-plan-phase 1 -- ship Phase 1 Foundation plan (ADPT-02 directory scaffolding)"

### Actions Taken
- Created routines-codex/.gitkeep (ADPT-02 sibling directory)
- Created routines-gemini/.gitkeep (ADPT-02 sibling directory)
- Created templates/.gitkeep (DOCS-02 placeholder for Phase 6)
```

Replace `HH:MM` with current approximate time in EST.

DO NOT include entries for types.ts / index.ts / slug.ts — those belong to other plans.
  </action>
  <verify>
    <automated>grep -c "routines-codex/.gitkeep" /Users/rahulmehta/Desktop/Projects/sleepwalker/docs/activity_log.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f docs/activity_log.md` returns 0
    - `grep -c "routines-codex/.gitkeep" docs/activity_log.md` returns `>= 1`
    - `grep -c "routines-gemini/.gitkeep" docs/activity_log.md` returns `>= 1`
    - `grep -c "templates/.gitkeep" docs/activity_log.md` returns `>= 1`
    - Entry starts with `## YYYY-MM-DD HH:MM EST`
    - Entry contains `### User Prompt` and `### Actions Taken`
  </acceptance_criteria>
  <done>Activity log updated with Plan 02 actions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem (repo root) | Plan 02 creates three new directories at repo root. No code runs. No user input processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | empty-directory durability | mitigate | `.gitkeep` files contain an explicit protective comment so Phase 3/6 contributors recognize them as intentional scaffolding and do not delete them. |
| T-01-02-02 | Information Disclosure | directory names | accept | Directory names (`routines-codex/`, `routines-gemini/`, `templates/`) are published in CLAUDE.md and ROADMAP.md already — no secret information in the names. |
| T-01-02-03 | Denial of Service | frozen v0.1 paths | mitigate | Plan 02 only creates NEW directories; tasks have explicit DO-NOT-TOUCH clauses for `routines-local/` and `routines-cloud/`. Plan 04 verifies zero diff via git. |
</threat_model>

<verification>
Phase 1 Plan 02 gate (run from repo root):

```bash
# Directory existence (three new siblings)
test -d routines-codex && test -d routines-gemini && test -d templates

# .gitkeep files present
test -f routines-codex/.gitkeep && test -f routines-gemini/.gitkeep && test -f templates/.gitkeep

# Protective comment in each
grep -l "Placeholder" routines-codex/.gitkeep routines-gemini/.gitkeep templates/.gitkeep

# v0.1 directories untouched (should list unchanged)
git status --porcelain routines-local/ routines-cloud/ | wc -l
# Expected: 0
```
</verification>

<success_criteria>
- Three new sibling directories exist at repo root: `routines-codex/`, `routines-gemini/`, `templates/`.
- Each contains exactly one `.gitkeep` file with the protective comment.
- v0.1 directories `routines-local/` and `routines-cloud/` are byte-identical (zero `git status` output).
- `docs/activity_log.md` has a new entry for Plan 02 actions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md` following the template in `$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- Directories created: 3 (routines-codex, routines-gemini, templates)
- `.gitkeep` files created: 3 (each with protective comment)
- v0.1 untouched confirmation (git status returns 0 lines for routines-local/ + routines-cloud/)
- Hand-off to Plan 03: "Phase 2 adapter bundles will be written into routines-codex/ and routines-gemini/; toBundleDir() in slug.ts resolves paths correctly against these now-existing siblings."
</output>
